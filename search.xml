<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>k-means算法</title>
    <url>/2021/08/01/k-means%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h5 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h5><p>据说是机器学习中最简单的一种方法，属于<strong>非监督学习</strong>这一类。通俗一点讲，给机器一堆数据，告诉机器要分成多少种类别，然后机器就根据算法进行各种操作。</p>
<h5 id="实现原理"><a href="#实现原理" class="headerlink" title="实现原理"></a>实现原理</h5><p>需要我们确定变量k，即最终 的数据会有几种类别。</p>
<p>总共有k个中心坐标，一开始可以随机赋值，通过计算目标点坐标与所有中心坐标的距离，将其归类在距离较近的类别中。</p>
<p>之后根据类别中的目标点计算平均值重新得到中心坐标，再重复上面的工作。</p>
<h5 id="距离公式"><a href="#距离公式" class="headerlink" title="距离公式"></a>距离公式</h5><p>常用的距离公式如欧式距离，马氏距离，曼哈顿距离。</p>
<p>借助科学计算库可以帮我们节省一些时间。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scipy.spatial.distance <span class="keyword">import</span> pdist</span><br><span class="line">pdist(np.array([dot[j], center[k]]), <span class="string">&#x27;euclidean&#x27;</span>)<span class="comment">#欧氏距离</span></span><br></pre></td></tr></table></figure>

<h5 id="小小示例"><a href="#小小示例" class="headerlink" title="小小示例"></a>小小示例</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> scipy.spatial.distance <span class="keyword">import</span> pdist</span><br><span class="line"></span><br><span class="line">dimension = <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成多个随机点，范围[0,10)</span></span><br><span class="line">dot_num = <span class="number">100</span></span><br><span class="line">dot = np.random.rand(dot_num,dimension)*<span class="number">10</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 生成多个随机中心点，范围[0,10)</span></span><br><span class="line">center_num = <span class="number">4</span></span><br><span class="line">center = np.random.rand(center_num,dimension)*<span class="number">10</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 循环迭代</span></span><br><span class="line">times = <span class="number">50</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(times):</span><br><span class="line">    plt.clf()</span><br><span class="line">    <span class="comment"># 数据映射</span></span><br><span class="line">    belong = np.zeros([dot_num])</span><br><span class="line">    <span class="comment"># 计算随机点与中心点的距离，将其归属于距离近的中心点</span></span><br><span class="line">    dist = np.zeros([dot_num,center_num])</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(dot_num):</span><br><span class="line">        <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(center_num):</span><br><span class="line">            dist[j][k] = pdist(np.array([dot[j], center[k]]), <span class="string">&#x27;euclidean&#x27;</span>)</span><br><span class="line">        belong[j] = np.argmax(dist[j])</span><br><span class="line">    <span class="comment"># 绘图</span></span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(center_num):</span><br><span class="line">        temp_dot = []</span><br><span class="line">        <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(dot_num):</span><br><span class="line">            <span class="keyword">if</span>(belong[k] == j):</span><br><span class="line">                temp_dot.append(dot[k])</span><br><span class="line">        <span class="comment"># 根据随机点求取均值，得出新的中心点</span></span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">len</span>(temp_dot) != <span class="number">0</span>):</span><br><span class="line">            temp_dot = np.array(temp_dot)</span><br><span class="line">            plt.scatter(temp_dot[:,<span class="number">0</span>],temp_dot[:,<span class="number">1</span>])</span><br><span class="line">            <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(dimension):</span><br><span class="line">                center[j][k] = np.<span class="built_in">sum</span>(temp_dot[:,k])/<span class="built_in">len</span>(temp_dot)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            center[j] = np.zeros([dimension])</span><br><span class="line">    <span class="comment"># print(center)</span></span><br><span class="line">    plt.pause(<span class="number">1</span>)</span><br><span class="line">    <span class="comment"># 满足迭代次数或中心点不再改变，结束命令</span></span><br></pre></td></tr></table></figure>

<h5 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h5><p>上述代码为动态实时更新画板内容，其中的关键代码如下</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">5</span>):</span><br><span class="line">    <span class="comment"># 清除画布</span></span><br><span class="line">    plt.clf()</span><br><span class="line">    <span class="comment"># 绘制散点</span></span><br><span class="line">    plt.scatter(temp_dot[:,<span class="number">0</span>],temp_dot[:,<span class="number">1</span>])</span><br><span class="line">    <span class="comment"># 暂停间隔</span></span><br><span class="line">    plt.pause(<span class="number">1</span>)</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>KVO与KVOController的介绍与实现原理</title>
    <url>/2021/06/27/KVO%E4%B8%8EKVOController%E7%9A%84%E4%BB%8B%E7%BB%8D%E4%B8%8E%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<h3 id="KVO的介绍"><a href="#KVO的介绍" class="headerlink" title="KVO的介绍"></a>KVO的介绍</h3><p>KVO全名Key Value Observing，用于观察键值的变化，在MVC开发模式中重要作用，常用于一对多的场景，基于KVC实现。</p>
<p>KVO一般代码实现分为三个步骤：</p>
<span id="more"></span>

<ul>
<li>添加观察</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@implementation Person</span><br><span class="line">- (void)observeNTESStock &#123;</span><br><span class="line">    [self.stock addObserver:self forKeyPath:@&quot;price&quot; options:NSKeyValueObservingOptionNew context:nil];</span><br><span class="line">&#125;</span><br><span class="line">@end</span><br></pre></td></tr></table></figure>

<ul>
<li>添加回调</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">- (void)observeValueForKeyPath:(NSString *)keyPath ofObject:(id)object change:(NSDictionary&lt;NSKeyValueChangeKey,id&gt; *)change context:(void *)context &#123;</span><br><span class="line">    // To Do Something···</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>移除观察</li>
</ul>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[<span class="keyword">self</span>.stock removeObserver:<span class="keyword">self</span> forKeyPath:<span class="string">@&quot;price&quot;</span>];</span><br></pre></td></tr></table></figure>

<h3 id="KVO的不足"><a href="#KVO的不足" class="headerlink" title="KVO的不足"></a>KVO的不足</h3><ul>
<li><p>当观察的键值比较多的时候，在回调的方法中会存在大量的if语句；</p>
</li>
<li><p>需要手动移除观察，操作不当容易Crash（在IOS 10及以下会发生）</p>
</li>
<li><p>可重复添加观察（导致多次回调），当移除观察方法 &gt; 添加观察方法的次数时，会crash</p>
</li>
</ul>
<h3 id="KVOController介绍"><a href="#KVOController介绍" class="headerlink" title="KVOController介绍"></a>KVOController介绍</h3><p>KVOController是FaceBook开源的基于KVO封装的框架，提高了代码的可读性和安全性。</p>
<img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210627190457920.png" alt="image-20210627190457920" style="zoom: 50%;" />

<p>引入KVOController以后，键值观察的实现将变得非常简单。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@interface ViewController ()</span><br><span class="line">@property (nonatomic, strong) FBKVOController *KVOController;</span><br><span class="line">@end</span><br><span class="line"></span><br><span class="line">@implementation ViewController</span><br><span class="line">- (void)viewDidLoad &#123;</span><br><span class="line">    [super viewDidLoad];</span><br><span class="line">    self.KVOController = [[FBKVOController alloc] initWithObserver:self];</span><br><span class="line">    Person *person = [[Person alloc] init];</span><br><span class="line">    [self.KVOController observe:person keyPath:@&quot;name&quot; options:NSKeyValueObservingOptionNew block:^(id  _Nullable observer, id  _Nonnull object, NSDictionary&lt;NSKeyValueChangeKey,id&gt; * _Nonnull change) &#123;</span><br><span class="line">        NSLog(@&quot;%@&quot;, change);</span><br><span class="line">    &#125;];</span><br><span class="line">    person.name = @&quot;Jane&quot;;</span><br><span class="line">&#125;</span><br><span class="line">@end</span><br><span class="line">=====输出=====</span><br><span class="line">&#123;</span><br><span class="line">    FBKVONotificationKeyPathKey = name;</span><br><span class="line">    kind = 1;</span><br><span class="line">    new = Jane;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>KVOController也对NSObject做了category，内置了KVOController属性。</p>
<h3 id="KVOController源码分析"><a href="#KVOController源码分析" class="headerlink" title="KVOController源码分析"></a>KVOController源码分析</h3><p>KVOController的源码非常少，一共五个文件。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210627192143311.png" alt="image-20210627192143311"></p>
<p>NSObject+FBKVOController主要是增加了NSObject的两个属性，区别在于是否持有被观察对象的强引用，它们都采用了懒加载。</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">NSObject</span> (<span class="title">FBKVOController</span>)</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) FBKVOController *KVOController;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) FBKVOController *KVOControllerNonRetaining;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line">  </span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> *<span class="built_in">NSObjectKVOControllerKey</span> = &amp;<span class="built_in">NSObjectKVOControllerKey</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> *<span class="built_in">NSObjectKVOControllerNonRetainingKey</span> = &amp;<span class="built_in">NSObjectKVOControllerNonRetainingKey</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">NSObject</span> (<span class="title">FBKVOController</span>)</span></span><br><span class="line">- (FBKVOController *)KVOController</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">id</span> controller = objc_getAssociatedObject(<span class="keyword">self</span>, <span class="built_in">NSObjectKVOControllerKey</span>);</span><br><span class="line">  </span><br><span class="line">  <span class="comment">// lazily create the KVOController</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="literal">nil</span> == controller) &#123;</span><br><span class="line">    controller = [FBKVOController controllerWithObserver:<span class="keyword">self</span>];</span><br><span class="line">    <span class="keyword">self</span>.KVOController = controller;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">return</span> controller;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)setKVOController:(FBKVOController *)KVOController</span><br><span class="line">&#123;</span><br><span class="line">  objc_setAssociatedObject(<span class="keyword">self</span>, <span class="built_in">NSObjectKVOControllerKey</span>, KVOController, OBJC_ASSOCIATION_RETAIN_NONATOMIC);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (FBKVOController *)KVOControllerNonRetaining</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">id</span> controller = objc_getAssociatedObject(<span class="keyword">self</span>, <span class="built_in">NSObjectKVOControllerNonRetainingKey</span>);</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">if</span> (<span class="literal">nil</span> == controller) &#123;</span><br><span class="line">    controller = [[FBKVOController alloc] initWithObserver:<span class="keyword">self</span> retainObserved:<span class="literal">NO</span>];</span><br><span class="line">    <span class="keyword">self</span>.KVOControllerNonRetaining = controller;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">return</span> controller;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)setKVOControllerNonRetaining:(FBKVOController *)KVOControllerNonRetaining</span><br><span class="line">&#123;</span><br><span class="line">  objc_setAssociatedObject(<span class="keyword">self</span>, <span class="built_in">NSObjectKVOControllerNonRetainingKey</span>, KVOControllerNonRetaining, OBJC_ASSOCIATION_RETAIN_NONATOMIC);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure>

<h4 id="对象封装"><a href="#对象封装" class="headerlink" title="对象封装"></a>对象封装</h4><p>KVOController把观察的键值路径、回调block等信息封装到一个对象中，内部重写和hash和isEqual方法。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210627194620904.png" alt="image-20210627194620904"></p>
<p>KVOController内部有三个属性，_observer为指向观察对象的弱引用。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/2017-03-06-KVOController.png-1000width" alt="KVOControlle"></p>
<h4 id="初始化与销毁代码"><a href="#初始化与销毁代码" class="headerlink" title="初始化与销毁代码"></a>初始化与销毁代码</h4><p>FBKVOController内的属性<code>_objectInfosMap</code> 根据retainObserved是YES还是NO，在初始化时选择对Key值进行强引用还是弱引用。<code>_objectInfosMap</code>的Key记录了被观察者，Value记录了被观察者的所有FBKVOInfo集合。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210627194253013.png" alt="image-20210627194253013"></p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/2017-03-06-objectInfosMap.png-1000width" alt="objectInfosMap"></p>
<h4 id="添加观察者"><a href="#添加观察者" class="headerlink" title="添加观察者"></a>添加观察者</h4><p>这里以Block的为例。首先将参数封装到FBKVOInfo中，然后调用内部的_observe:info方法</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210627210553489.png" alt="image-20210627210553489"></p>
<p>内部的_observe:info方法，将FBKVOInfo加入到对应object的集合中。最后调用了_FBKVOSharedController的方法，这个FBKVOSharedController采用单例实现，所有的FBKVOController共享。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210627210720980.png" alt="image-20210627210720980"></p>
<p>这个FBKVOSharedController内部同样有一个属性，用来存放FBKVOInfo，持有的是弱引用。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210627211759816.png" alt="image-20210627211759816"></p>
<p>在这里，可以看到调用了原生的addObserver方法了。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210627211922146.png" alt="image-20210627211922146"></p>
<p>Observer指定为self，所以当键值发生变化时，就会回调这个FBKVOSharedController里面的方法。这里会依次判断FBKVOInfo中是否有Block、Action、默认回调方法，选择一个执行。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210627212422089.png" alt="image-20210627212422089"></p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/2017-03-06-KVOSharedController.png-1000width" alt="KVOSharedControlle"></p>
<p>移除观察者的流程和上面类似，这里不再展开。</p>
<h3 id="KVOController注意事项"><a href="#KVOController注意事项" class="headerlink" title="KVOController注意事项"></a>KVOController注意事项</h3><p>虽然KVOController已经修复了KVO的一些问题，但是如果用得不恰当，还是会有Crash发生。如果在dealloc中第一次调用了KVOController，就会导致Crash。</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">SecondViewController</span></span></span><br><span class="line">- (<span class="keyword">void</span>)viewDidLoad &#123;</span><br><span class="line">    [<span class="keyword">super</span> viewDidLoad];</span><br><span class="line">    <span class="keyword">if</span>(arc4random_uniform(<span class="number">4</span>) == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@&quot;%@&quot;</span>, <span class="keyword">self</span>.KVOController);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)dealloc &#123;</span><br><span class="line">    [<span class="keyword">self</span>.KVOController unobserveAll];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure>

<p>原因也比较简单，KVOController采用了懒加载，在init方法中会把当前对象赋值给其内部属性observer，但是当前对象已经到了dealloc的处理流程，无法赋值。</p>
<p>不过一般也不需要在当前对象的dealloc方法中移除监听，因为KVOController的dealloc方法已经做了这样的处理。</p>
<h3 id="KVO的基本原理"><a href="#KVO的基本原理" class="headerlink" title="KVO的基本原理"></a>KVO的基本原理</h3><p>KVOController本质是对原有KVO的封装，那么原有KVO又是如何实现的呢？</p>
<p>当对象添加了KVO监听之后，系统会修改对象的isa指针，这个过程称为isa-swizzling。新isa指针指向的类是原来的子类，通过运行时机制生成，其中重写了set方法。set方法调用了 Fundation框架中C语言函数 _NSsetIntValueAndNotifyset，这个方法的大致调用逻辑为：调用willChangeValueForKey方法，然后调用原来的set方法，最后调用didChangeValueForKey。其中didChangeValueForKey方法内部调用了observeValueForKeyPath:ofObject:change:context:监听方法。</p>
<p><strong>未添加KVO时</strong></p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/926.png" alt="img"></p>
<p><strong>添加KVO之后</strong></p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/1024.png" alt="img"></p>
<p>下面通过代码调试来验证我们的猜想。</p>
<p>创建一个Person类，里面只有一个age属性</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Person</span> : <span class="title">NSObject</span></span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="keyword">int</span> age;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line">  </span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">Person</span></span></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure>

<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210628003922406.png" alt="image-20210628003922406"></p>
<p>我们可以看到，在添加KVO监听之后，p1的setAge方法的IMP已经变了。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210628004138336.png" alt="image-20210628004138336"></p>
<p>把这两个IMP打印出来，发现p1的已经变成了NSSetIntValueAndNotify。</p>
<p>下面我们看一下p1指向的类是否已经改变，并查看该类的内部结构。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210628005405029.png" alt="image-20210628005405029"></p>
<p>可以看到，p1指向的类已经变为NSKVONotifying_Person，里面有四个方法。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210628005924792.png" alt="image-20210628005924792"></p>
<p>为什么有class方法，主要是为了对外屏蔽这个子类的存在。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210628010126761.png" alt="image-20210628010126761"></p>
<p>下面验证didChangeValueForKey:内部会调用observer的observeValueForKeyPath:ofObject:change:context:方法</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210628011008650.png" alt="image-20210628011008650"></p>
<p>知道这个特点后，我们甚至可以在不更新属性的情况下调用KVO回调方法。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210628011343881.png" alt="image-20210628011343881"></p>
<p>另外，设置成员变量 _age 由于不会触发 setter 方法 ,因此不会触发 KVO 相关的代码。</p>
<h3 id="自己实现KVO"><a href="#自己实现KVO" class="headerlink" title="自己实现KVO"></a>自己实现KVO</h3><h3 id="疑问"><a href="#疑问" class="headerlink" title="疑问"></a>疑问</h3><p>当观察的键值比较负责，比如Person的dog.name的时候，动态生成的类是Person还是Dog</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><h3 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h3><p><a href="https://mp.weixin.qq.com/s?__biz=MzUxODg0MzU2OQ==&mid=2247484301&idx=1&sn=14488ad70e79f5e9f91b026e47bbb13c&chksm=f983fe53cef47745ec9f768f4aff2e4e93d34083d4dd66da5fd18ce0ee44fc488df3a0ee12e5&token=1147085013&lang=zh_CN#rd">你的KVO用对了吗</a></p>
<p><a href="https://github.com/facebookarchive/KVOController">KVOController源码</a></p>
<p><a href="https://developer.apple.com/library/mac/documentation/Cocoa/Conceptual/KeyValueObserving/KeyValueObserving.html">苹果官方KVO文档</a></p>
<p><a href="https://draveness.me/kvocontroller/">如何优雅地使用KVO</a></p>
<p><a href="https://blog.csdn.net/kyl282889543/article/details/93859511">KVO底层实现原理</a></p>
]]></content>
  </entry>
  <entry>
    <title>二分查找</title>
    <url>/2021/08/01/%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE/</url>
    <content><![CDATA[<h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>二分查找，又称折半查找。顾名思义，就是在一堆<strong>有序排列</strong>的元素中查找某一指定数值。返回数值对应的下标或-1（查不到）。</p>
<h2 id="场景模拟"><a href="#场景模拟" class="headerlink" title="场景模拟"></a>场景模拟</h2><p>看其他文章都是千篇一律的猜数字，我们换一换风格，来猜一猜价格吧。这不是一样的吗？</p>
<p>比如说现在让你猜一件商品的价格，已知商品的价格在1-1000元之间，且为整数，现在让你尽快猜中商品的价格，你要怎么做呢？（你每猜一个价格，都能得知这个价格与正确价格相比是相同的，还是小了或大了）</p>
<p>鲁莽的方法：从1元开始猜到1000元，总会猜到的。运气好的话，价格刚好是1元，猜一次就中了。运气不好的话，价格是1000元，要猜1000次才能猜中。</p>
<p><strong>这种方法其实没有充分利用猜价格得到的反馈信息，即从1元开始猜到1000元的过程中，你得到的反馈只有正确或者小了，绝对不会出现大了。</strong></p>
<p>睿智的方法：既然价格在1元到1000元之间，那我就猜500元，即（1+1000）/2。这时可能的反馈信息分别有</p>
<p>比正确价格小：这样我们就知道商品的价格不在1元到500元之间，而是在501元到1000元之间。</p>
<p>与正确价格相同：即我们猜中了，所以就直接返回这个结果就可以了。</p>
<p>比正确价格大：所以商品价格不在500元到1000元之间，而是在1元到499元之间。</p>
<p><strong>不难看出，只经过一次猜测，可能猜中或者把猜测范围缩小一半。后面用同样的方式猜测，则又可以将猜测范围缩小一半。</strong>这个不就是递归吗？虽然用循环也可以做</p>
<p><strong>用二分查找的时间复杂度为O（logN）</strong></p>
<h2 id="代码模板"><a href="#代码模板" class="headerlink" title="代码模板"></a>代码模板</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">binarySearch</span><span class="params">(vector&lt;<span class="keyword">int</span>&gt; arr,<span class="keyword">int</span> aim)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * 参数说明：arr即查找的容器序列，aim为需要查找的元素</span></span><br><span class="line"><span class="comment">     * 要求：arr容器需保证元素升序</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">int</span> left=<span class="number">0</span>,right=arr.<span class="built_in">size</span>()<span class="number">-1</span>,mid=(left+right)/<span class="number">2</span>;</span><br><span class="line">    <span class="keyword">while</span>(left&lt;=right)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(arr[mid]&lt;aim)&#123;</span><br><span class="line">            left=mid+<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(arr[mid]==aim)&#123;</span><br><span class="line">            cout&lt;&lt;<span class="string">&quot;找到了&quot;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(arr[mid]&gt;aim)&#123;</span><br><span class="line">            right=mid<span class="number">-1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        mid=(left+right)/<span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;找不到&quot;</span>;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    vector&lt;<span class="keyword">int</span>&gt; arr;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">10</span>;i++)&#123;</span><br><span class="line">        arr.<span class="built_in">push_back</span>(i*<span class="number">2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">binarySearch</span>(arr,<span class="number">7</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>二分</tag>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>字典树</title>
    <url>/2021/08/01/%E5%AD%97%E5%85%B8%E6%A0%91/</url>
    <content><![CDATA[<blockquote>
<p> 字典树一般用于字符串的匹配，当数量多的时候效率还是挺明显的。</p>
</blockquote>
<p> 这里以一道洛谷的题为例子，<a href="https://www.luogu.org/problem/P2580">https://www.luogu.org/problem/P2580</a></p>
<h3 id="题目的大致意思时，先给你n个字符串，然后再给你m个字符串，求这m个字符串是否在n个字符串中出现过或是否重复出现了。"><a href="#题目的大致意思时，先给你n个字符串，然后再给你m个字符串，求这m个字符串是否在n个字符串中出现过或是否重复出现了。" class="headerlink" title="题目的大致意思时，先给你n个字符串，然后再给你m个字符串，求这m个字符串是否在n个字符串中出现过或是否重复出现了。"></a>题目的大致意思时，先给你n个字符串，然后再给你m个字符串，求这m个字符串是否在n个字符串中出现过或是否重复出现了。</h3><hr>
<p>若采用朴素一对一的比较方式，那时间复杂度就是O(n <strong>X</strong> m <strong>X</strong> ‘字符串长度’)，所以还是算了。</p>
<p>解决的方法便是利用字典树，将n个字符串搭建成一个字典树。</p>
<p><strong>什么是字典树？？？？比如字符串（ab,ac,b,bca,bcd）构成的字典树如下</strong></p>
<p><img src="https://img-blog.csdnimg.cn/20190825170358538.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzE3MTkwMTIx,size_16,color_FFFFFF,t_70" alt="img"><img src="data:image/gif;base64,R0lGODlhAQABAPABAP///wAAACH5BAEKAAAALAAAAAABAAEAAAICRAEAOw==" alt="点击并拖拽以移动"></p>
<p><strong>红色标记的结点即该结点为某一字符串的结束结点，注意字典树的根结点不存任何字符</strong></p>
<blockquote>
<p>构建完字典树后，后面的操作就简单了。待查询的m个字符串依次从根节点开始查询，<strong>当前结点存在该字符，则更新当前位置到该字符所在结点，否则就证明查询不到。到最后一个字符匹配且该结点是结束结点，说明查询成功。</strong></p>
</blockquote>
<p>下面是题目的代码</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">int</span> n,m,cnt;</span><br><span class="line">string s;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> vis[<span class="number">26</span>];</span><br><span class="line">    <span class="keyword">int</span> state;<span class="comment">//0 普通节点 1 结束节点 2 结束且已访问</span></span><br><span class="line">&#125;tried[<span class="number">500010</span>];</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Build_tree</span><span class="params">(string s)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len=s.<span class="built_in">length</span>();</span><br><span class="line">    <span class="keyword">int</span> now=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(tried[now].vis[s[i]-<span class="string">&#x27;a&#x27;</span>]==<span class="number">0</span>)&#123;</span><br><span class="line">            tried[now].vis[s[i]-<span class="string">&#x27;a&#x27;</span>]=++cnt;</span><br><span class="line">        &#125;</span><br><span class="line">        now=tried[now].vis[s[i]-<span class="string">&#x27;a&#x27;</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    tried[now].state=<span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Query_str</span><span class="params">(string s)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len=s.<span class="built_in">length</span>();</span><br><span class="line">    <span class="keyword">int</span> now=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(tried[now].vis[s[i]-<span class="string">&#x27;a&#x27;</span>])&#123;</span><br><span class="line">            now=tried[now].vis[s[i]-<span class="string">&#x27;a&#x27;</span>];</span><br><span class="line">            <span class="keyword">if</span>(i==len<span class="number">-1</span>&amp;&amp;tried[now].state==<span class="number">1</span>)&#123;</span><br><span class="line">                cout&lt;&lt;<span class="string">&quot;OK&quot;</span>&lt;&lt;endl;</span><br><span class="line">                tried[now].state=<span class="number">2</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(i==len<span class="number">-1</span>&amp;&amp;tried[now].state==<span class="number">2</span>)&#123;</span><br><span class="line">                cout&lt;&lt;<span class="string">&quot;REPEAT&quot;</span>&lt;&lt;endl;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            cout&lt;&lt;<span class="string">&quot;WRONG&quot;</span>&lt;&lt;endl;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cin&gt;&gt;n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;n;i++)&#123;</span><br><span class="line">        cin&gt;&gt;s;</span><br><span class="line">        <span class="built_in">Build_tree</span>(s);</span><br><span class="line">    &#125;</span><br><span class="line">    cin&gt;&gt;m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;m;i++)&#123;</span><br><span class="line">        cin&gt;&gt;s;</span><br><span class="line">        <span class="built_in">Query_str</span>(s);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
        <tag>字符串匹配</tag>
      </tags>
  </entry>
  <entry>
    <title>快速幂</title>
    <url>/2021/08/01/%E5%BF%AB%E9%80%9F%E5%B9%82/</url>
    <content><![CDATA[<p>​    今天介绍一个在中低难度算法竞赛中经常碰到的算法—快速幂，于此相匹配的还有一个叫矩阵快速幂的东西。不过这篇文章只介绍快速幂，矩阵快速幂只是把两个数的乘法换成两个矩阵的乘法而已。</p>
<p>快速幂，顾名思义，就是快速求解某一个数的指数次方结果为多少。</p>
<h2 id="普通求解"><a href="#普通求解" class="headerlink" title="普通求解"></a>普通求解</h2><p>如让你求2^5次方，你可以很快计算出结果为2*2*2*2*2=32。</p>
<blockquote>
<p>这种方式在计算机中可以使用pow函数解决，即pow（2,5）=32。引入头文件cmath就行了，当然你也可以手写。</p>
</blockquote>
<p>pow函数的实现方式大致如下</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"><span class="function">ll <span class="title">pow</span><span class="params">(ll a,ll b,ll c)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ll d=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(b--)&#123;</span><br><span class="line">        d=d*a%c;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> d;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>不难看出，这种计算方式的速度是由b决定的，即时间复杂度为O（b），而如果b很大的，那就超时了啊！</strong></p>
<hr>
<h2 id="快速幂"><a href="#快速幂" class="headerlink" title="快速幂"></a>快速幂</h2><p>下面就进入本节的主要内容，用快速幂求解，这其实是对二进制的巧妙使用。</p>
<p>讲一个例子，现在要求3^11，按上述方法需要计算11次，但快速幂可以将求3^11简化为(3^8)<em>(3^2)</em>(3^1)，这样就只计算了3次，其实总共比较了4次，往下看就懂了。</p>
<p>来一波分析：</p>
<ol>
<li>将指数11转化为二进制，即00…001011（32位，前面的0我就省略不写了，太多容易看乱）</li>
<li>从右到左开始，32位的01串我们在下面给它们一个数值标识，从3^1开始，以平方的方式不断递增，如下面的表格所示（注意只给出最后边的6位，int类型总共应该有32位） </li>
</ol>
<table>
<thead>
<tr>
<th>0</th>
<th>0</th>
<th>1</th>
<th>0</th>
<th>1</th>
<th>1</th>
</tr>
</thead>
<tbody><tr>
<td>3^32</td>
<td>3^16</td>
<td>3^8</td>
<td>3^4</td>
<td>3^2</td>
<td>3^1</td>
</tr>
</tbody></table>
<blockquote>
<p> 其实下面的指数也可以有另外一种方式理解，即当前位置的1表示十进制的哪个数，指数就是哪个。</p>
</blockquote>
<p>所以下面的操作就很简单了，3^11=3^(8+2+1)，为什么要拆成这几个数呢，因为他们就是11转成二进制后，对应的1的位置数值的表示啊（上面的表格），而这些位置的1代表的数加起来就是11. </p>
<p>那么计算的思路就是，读取11的二进制数，若该位置为1，就乘上该位置所代表的数，若为0则不作处理。</p>
<p><strong>时间复杂度从O(N)降到了O(logN)</strong></p>
<blockquote>
<p>具体方法：将11与1做按位与位运算，若结果为1，说明11二进制最后的数为1，所以乘上。</p>
<p>然后下一步我们让11做右移位运算，即将32个01集体往右移动1位，这时最左侧会补上0，而最右侧的1已经不见了。相当于继续比较下一位，若当前数值为0，即二进制32个都为0，说明已经没有1了，就可以结束循环。</p>
</blockquote>
<h2 id="具体的代码实现"><a href="#具体的代码实现" class="headerlink" title="具体的代码实现"></a>具体的代码实现</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"></span><br><span class="line"><span class="function">ll <span class="title">quickPow</span><span class="params">(ll a,ll b,ll c)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ll d=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(b)&#123;</span><br><span class="line">        <span class="keyword">if</span>(b&amp;<span class="number">1</span>)&#123;</span><br><span class="line">            d=d*a%c;</span><br><span class="line">        &#125;</span><br><span class="line">        a=a*a%c;<span class="comment">//更新当前位置代表的数</span></span><br><span class="line">        b=b&gt;&gt;<span class="number">1</span>;<span class="comment">//右移1位</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> d;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;10的1亿次方mod3的结果为: &quot;</span>&lt;&lt;<span class="built_in">quickPow</span>(<span class="number">10</span>,<span class="number">1e9</span>,<span class="number">3</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="矩阵快速幂"><a href="#矩阵快速幂" class="headerlink" title="矩阵快速幂"></a>矩阵快速幂</h2><p>这里放一下矩阵快速幂的模板</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">typedef long long ll;</span><br><span class="line">const int mod=1e9+7;</span><br><span class="line">const int MaxN=105;//矩阵大小</span><br><span class="line">struct Mat&#123;</span><br><span class="line">    ll m[MaxN][MaxN];</span><br><span class="line">&#125;ans,a;//结果与输入矩阵</span><br><span class="line">Mat Mul(Mat a,Mat b,int n)&#123;</span><br><span class="line">    Mat c;</span><br><span class="line">    memset(c.m,0,sizeof(c.m));</span><br><span class="line">    <span class="keyword">for</span>(int i=1;i&lt;=n;i++)</span><br><span class="line">        <span class="keyword">for</span>(int j=1;j&lt;=n;j++)</span><br><span class="line">            <span class="keyword">for</span>(int k=1;k&lt;=n;k++)</span><br><span class="line">                c.m[i][j]=(c.m[i][j]+(a.m[i][k]*b.m[k][j])%mod)%mod;</span><br><span class="line">    <span class="built_in">return</span> c;</span><br><span class="line">&#125;</span><br><span class="line">Mat _power(Mat a,ll b,int n)&#123;</span><br><span class="line">    <span class="keyword">for</span>(int i=1;i&lt;=n;i++)</span><br><span class="line">        ans.m[i][i]=1;</span><br><span class="line">    <span class="keyword">while</span>(b)&#123;</span><br><span class="line">        <span class="keyword">if</span>(b&amp;1)</span><br><span class="line">            ans=Mul(ans,a,n);</span><br><span class="line">        a=Mul(a,a,n);</span><br><span class="line">        b&gt;&gt;=1;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">return</span> ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>算法</category>
      </categories>
  </entry>
  <entry>
    <title>搭建个人博客</title>
    <url>/2021/06/24/%E6%90%AD%E5%BB%BA%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2/</url>
    <content><![CDATA[<h3 id="服务器选购"><a href="#服务器选购" class="headerlink" title="服务器选购"></a>服务器选购</h3><p>我这次采用的是腾讯云，因为我有几百块的代金劵，哈哈。</p>
<p>操作系统：Ubuntu 20.04 LTS</p>
<p>硬件配置：1核 2GB 2Mbps</p>
<h3 id="系统配置"><a href="#系统配置" class="headerlink" title="系统配置"></a>系统配置</h3><p>通过SSH获取密码登录系统，然后需要修改一下DNS服务器的配置。</p>
<p>默认好像开启了systemd-resolved这个服务，一开始一直解析不了DNS。</p>
<blockquote>
<p>关于systemd-resolved，可以参考这篇文章：<a href="https://www.freedomwolf.cc/2020/01/dns_by_systemd/">https://www.freedomwolf.cc/2020/01/dns_by_systemd/</a></p>
</blockquote>
<p>在<code>/etc/systemd/resolved.conf</code>中，把<code>#DNS=</code> 改为<code>DNS=1.1.1.1</code>，然后重启服务就可以了。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo systemctl restart systemd-resolved.service</span><br></pre></td></tr></table></figure>

<p>然而，现在是可以ping <a href="http://www.baidu.com了,但是tencent内部的一些域名就解析不了了./">www.baidu.com了，但是tencent内部的一些域名就解析不了了。</a></p>
<p>当我执行sudo apt-get update的时候，会出现下面这段内容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Err:1 http://mirrors.tencentyun.com/ubuntu focal InRelease</span><br><span class="line">  Something wicked happened resolving &#x27;mirrors.tencentyun.com:http&#x27; (-5 - No address associated with hostname)</span><br><span class="line">Err:2 http://mirrors.tencentyun.com/ubuntu focal-security InRelease</span><br><span class="line">  Something wicked happened resolving &#x27;mirrors.tencentyun.com:http&#x27; (-5 - No address associated with hostname)</span><br><span class="line">Err:3 http://mirrors.tencentyun.com/ubuntu focal-updates InRelease</span><br><span class="line">  Something wicked happened resolving &#x27;mirrors.tencentyun.com:http&#x27; (-5 - No address associated with hostname)</span><br><span class="line">Reading package lists... Done            </span><br><span class="line">W: Failed to fetch http://mirrors.tencentyun.com/ubuntu/dists/focal/InRelease  Something wicked happened resolving &#x27;mirrors.tencentyun.com:http&#x27; (-5 - No address associated with hostname)</span><br><span class="line">W: Failed to fetch http://mirrors.tencentyun.com/ubuntu/dists/focal-security/InRelease  Something wicked happened resolving &#x27;mirrors.tencentyun.com:http&#x27; (-5 - No address associated with hostname)</span><br><span class="line">W: Failed to fetch http://mirrors.tencentyun.com/ubuntu/dists/focal-updates/InRelease  Something wicked happened resolving &#x27;mirrors.tencentyun.com:http&#x27; (-5 - No address associated with hostname)</span><br><span class="line">W: Some index files failed to download. They have been ignored, or old ones used instead.</span><br></pre></td></tr></table></figure>

<p>一种方法是修改apt的镜像源，在/etc/apt/sources.list中配置其他镜像地址</p>
<p>另外就是使用腾讯的DNS服务器了。在<a href="https://cloud.tencent.com/document/product/213/5225%E8%BF%99%E9%87%8C%E6%89%BE%E3%80%82">https://cloud.tencent.com/document/product/213/5225这里找。</a></p>
<p>我的是广州六区，找不到，问了一下客服，选择了“私有网络-所有地域”这个类型的ip地址，然后就可以了。</p>
<h3 id="Wordpress的搭建"><a href="#Wordpress的搭建" class="headerlink" title="Wordpress的搭建"></a>Wordpress的搭建</h3><p>这个根据某位博主的指引，找到了一个不错的视频教程</p>
<p><a href="https://www.bilibili.com/video/BV1j4411C7Qf">https://www.bilibili.com/video/BV1j4411C7Qf</a></p>
<p>之前用WordPress搭建了一遍，但是感觉WordPress对代码和markdown的支持不太好，后面就改换Hexo了。</p>
<p>如何安装Hexo可以在官网得到详细的教程，所以这里不打算展开讲，下面讲讲官网没提到的。</p>
<h3 id="官网有的内容"><a href="#官网有的内容" class="headerlink" title="官网有的内容"></a>官网有的内容</h3><ul>
<li>如何安装Hexo框架</li>
<li>如何建网站、建文章、本地发布</li>
</ul>
<h3 id="我要讲的"><a href="#我要讲的" class="headerlink" title="我要讲的"></a>我要讲的</h3><h4 id="博客主题"><a href="#博客主题" class="headerlink" title="博客主题"></a>博客主题</h4><p>这里我选择了NexT，比较简洁，地址：<a href="https://theme-next.js.org,网站里面也介绍了如何配置各种东西./">https://theme-next.js.org，网站里面也介绍了如何配置各种东西。</a></p>
<blockquote>
<p>如果你不清楚某一个配置项如何实现的，可以在评论区留言</p>
</blockquote>
<h4 id="增加分类和标签页面"><a href="#增加分类和标签页面" class="headerlink" title="增加分类和标签页面"></a>增加分类和标签页面</h4><p>在source里面增加categories和tags两个文件夹，里面都添加一个index.md文件，内容分别为：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: categories</span><br><span class="line">date: 2021-06-13 10:16:14</span><br><span class="line">type: categories</span><br><span class="line">---</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: tags</span><br><span class="line">date: 2021-06-13 09:10:25</span><br><span class="line">type: tags</span><br><span class="line">---</span><br></pre></td></tr></table></figure>

<p>最后记得在NexT的配置中开启这两个，比如我当前的配置如下：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">menu:</span></span><br><span class="line">  <span class="attr">home:</span> <span class="string">/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-home</span></span><br><span class="line">  <span class="comment">#about: /about/ || fa fa-user</span></span><br><span class="line">  <span class="attr">tags:</span> <span class="string">/tags/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-tags</span></span><br><span class="line">  <span class="attr">categories:</span> <span class="string">/categories/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-th</span></span><br><span class="line">  <span class="attr">archives:</span> <span class="string">/archives/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-archive</span></span><br><span class="line">  <span class="comment">#schedule: /schedule/ || fa fa-calendar</span></span><br><span class="line">  <span class="comment">#sitemap: /sitemap.xml || fa fa-sitemap</span></span><br><span class="line">  <span class="comment">#commonweal: /404/ || fa fa-heartbeat</span></span><br></pre></td></tr></table></figure>

<h4 id="部署到服务器上"><a href="#部署到服务器上" class="headerlink" title="部署到服务器上"></a>部署到服务器上</h4><p>这里采用Git的方式部署，首先需要安装Git部署插件</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<p>本地配置添加（IP改为你的服务器地址，同时要先配置好SSH，比如我本地配置了SSH免密登陆服务器，不然每次都需要输入密码，当然这也看你个人意愿）</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">ubuntu@11.22.33.44:/home/ubuntu/hexo</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">master</span></span><br></pre></td></tr></table></figure>

<p>然后在服务器上面创建一个hexo的git仓库</p>
<blockquote>
<p>写博客的几个步骤：</p>
<p>hexo new ‘new blog title’</p>
<p>写内容…</p>
<p>hexo clean</p>
<p>hexo generate</p>
<p>hexo deploy</p>
</blockquote>
<p>如果你按我上面说的，当你deploy的时候，会出错</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">On branch master</span><br><span class="line">nothing to commit, working tree clean</span><br><span class="line">Enumerating objects: 90, done.</span><br><span class="line">Counting objects: 100% (90/90), done.</span><br><span class="line">Delta compression using up to 8 threads</span><br><span class="line">Compressing objects: 100% (77/77), done.</span><br><span class="line">Writing objects: 100% (90/90), 54.96 KiB | 3.92 MiB/s, done.</span><br><span class="line">Total 90 (delta 15), reused 0 (delta 0), pack-reused 0</span><br><span class="line">remote: error: refusing to update checked out branch: refs/heads/master</span><br><span class="line">remote: error: By default, updating the current branch in a non-bare repository</span><br><span class="line">remote: is denied, because it will make the index and work tree inconsistent</span><br><span class="line">remote: with what you pushed, and will require &#x27;git reset --hard&#x27; to match</span><br><span class="line">remote: the work tree to HEAD.</span><br><span class="line">remote: </span><br><span class="line">remote: You can set the &#x27;receive.denyCurrentBranch&#x27; configuration variable</span><br><span class="line">remote: to &#x27;ignore&#x27; or &#x27;warn&#x27; in the remote repository to allow pushing into</span><br><span class="line">remote: its current branch; however, this is not recommended unless you</span><br><span class="line">remote: arranged to update its work tree to match what you pushed in some</span><br><span class="line">remote: other way.</span><br><span class="line">remote: </span><br><span class="line">remote: To squelch this message and still keep the default behaviour, set</span><br><span class="line">remote: &#x27;receive.denyCurrentBranch&#x27; configuration variable to &#x27;refuse&#x27;.</span><br><span class="line">To 159.75.249.140:/home/ubuntu/hexo</span><br><span class="line"> ! [remote rejected] HEAD -&gt; master (branch is currently checked out)</span><br><span class="line">error: failed to push some refs to &#x27;159.75.249.140:/home/ubuntu/hexo&#x27;</span><br><span class="line">FATAL &#123;</span><br><span class="line">  err: Error: Spawn failed</span><br><span class="line">      at ChildProcess.&lt;anonymous&gt; (/Users/jieyingzhuang/Desktop/blog/node_modules/hexo-util/lib/spawn.js:51:21)</span><br><span class="line">      at ChildProcess.emit (node:events:394:28)</span><br><span class="line">      at Process.ChildProcess._handle.onexit (node:internal/child_process:290:12) &#123;</span><br><span class="line">    code: 1</span><br><span class="line">  &#125;</span><br><span class="line">&#125; Something&#x27;s wrong. Maybe you can find the solution here: %s https://hexo.io/docs/troubleshooting.html</span><br></pre></td></tr></table></figure>

<p>你可以按照提示输入命令<code>git config receive.denyCurrentBranch ignore</code>解决，这里我采用另一种方法解决：</p>
<p><strong>创建裸仓库，和代码文件分开存放</strong></p>
<p>创建一个叫hexo.git的裸仓库，所以hexo的配置信息也要换成hexo.git，创建hexo空文件夹。然后添加钩子post-receive并设置为可执行文件，里面的内容为</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git --work-tree=/home/ubuntu/hexo --git-dir=/home/ubuntu/hexo.git checkout -f</span><br></pre></td></tr></table></figure>

<p>这样，当你deploy的时候，会把网站的内容到存到hexo文件夹中。后面你还需要配置服务器，用Apache或者Nginx，然后指定网站的根目录为hexo，即可正常访问。</p>
<h3 id="没讲的东西"><a href="#没讲的东西" class="headerlink" title="没讲的东西"></a>没讲的东西</h3><p>感觉属于其他方面的知识，我就没有细说。这些也可以找其他博客看看。</p>
<ul>
<li>SSH免密登陆</li>
<li>配置Nginx服务器（配置SSL证书）</li>
<li>Hexo具体的配置信息（主要看各自喜好）</li>
</ul>
]]></content>
      <categories>
        <category>教程</category>
      </categories>
      <tags>
        <tag>教程</tag>
      </tags>
  </entry>
  <entry>
    <title>简述垃圾回收算法</title>
    <url>/2021/08/02/%E7%AE%80%E8%BF%B0%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h3 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h3><p>垃圾回收(Garbage Collection),简称GC,主要是对那些已经分配的空间进行回收利用,实现再分配.</p>
<p>比如下面的Java代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	List&lt;Integer&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">	list.add(<span class="number">1</span>);</span><br><span class="line">	System.out.println(list);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在这个函数中,我创建了一个ArrayList对象,Java则在内存堆上为其分配空间.但是一旦出了这个函数,我们就再也访问不到这块空间了,如果不进行回收,那么内存堆的这个位置就浪费了.</p>
<p><strong>所以GC要完成的主要有两件事:</strong></p>
<ul>
<li>找到内存中的垃圾</li>
<li>回收这部分垃圾,以便之后可以再分配</li>
</ul>
<h3 id="关于对象在堆上的布局"><a href="#关于对象在堆上的布局" class="headerlink" title="关于对象在堆上的布局"></a>关于对象在堆上的布局</h3><p>一般一个对象在堆中存储两部分信息,一部分是对象的元数据,比如对象的大小,hashcode的值以及用来标识垃圾回收的信息等.另一部分就是对象的具体数据.这部分主要分两种,如果是基本数据类型,那么这个位置存的就是它的值.如果不是,那么这个位置存放的是指向具体内存地址的指针.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a5aec2575de6280d7fb745cde167c8dc.png" alt="image-20210322220230929"></p>
<h3 id="如何找到内存中的垃圾"><a href="#如何找到内存中的垃圾" class="headerlink" title="如何找到内存中的垃圾"></a>如何找到内存中的垃圾</h3><p>这里采用==可达性分析==,首先我们会将一些对象作为根,这些对象我们把它标记为可达的,然后由这些根所能指向的其他对象,我们也标记为可达…这样一直传递下去,到最后,我们可以遍历整个堆,然后把那些不可达的对象给回收掉.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/baf321c73a321a69339a6223a35858d8.png" alt="image-20210322222228195"></p>
<p>哪些对象可以作为根呢 —&gt; 调用栈、寄存器以及全局变量</p>
<p>不过,这些根可能还有潜在的问题. <strong>怎么判断这个值是指针的地址还是一个基本数据类型呢</strong>?</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/904c4bbb0ff02745c26d5e8cabccbb42.png" alt="image-20210322232620392"></p>
<p>就概率上讲,数值刚好位于堆地址范围内,且刚好是对象头的地址,情况还是比较少的. 我们可以将错就错,不过在这种情况下,可能一个本该回收的对象就被我们列为根了.</p>
<blockquote>
<p>当存在貌似指针的非指针时，保守式GC会把被引用的对象错误识别为活动对象。如果这个对象存在大量的子对象，那么它们一律都会被看成活动对象</p>
</blockquote>
<p>要严格区别指针与非指针,需要语言处理程序的一些支持.这一块不作展开.</p>
<h3 id="GC算法评价指标"><a href="#GC算法评价指标" class="headerlink" title="GC算法评价指标"></a>GC算法评价指标</h3><ul>
<li>吞吐量</li>
</ul>
<p>如堆的大小为HEAP_SIZE,GC总共花费的时间为（A+B+C),则吞吐量为HEAP_SIZE/（A+B+C）。</p>
<ul>
<li>最大暂停时间</li>
</ul>
<p>最大暂停时间指的是“因执行GC而暂停执行mutator的最长时间”,比如下图指的就是B. 如果暂停时间太长,就会出现Stop-The-World</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a6884a135cffa7f0a5dfbd38e63963a0.png" alt="image-20210322223611521"></p>
<ul>
<li>堆使用效率</li>
</ul>
<p>不同的GC算法,会在堆中对象的对象头存放一些信息,大小不一,或者会将堆做划分.</p>
<ul>
<li>访问的局部性</li>
</ul>
<p>我们知道计算机是有高速缓存这么个东西.一般而言,具有引用关系的对象之间通常很可能存在连续访问的情况。所以如果在堆中这两个对象存放位置连续,那当读取第一个对象时,后面的对象也跟着被读进了缓存,后续访问的速度就加快了.</p>
<h3 id="GC算法介绍"><a href="#GC算法介绍" class="headerlink" title="GC算法介绍"></a>GC算法介绍</h3><h4 id="标记清除算法"><a href="#标记清除算法" class="headerlink" title="标记清除算法"></a>标记清除算法</h4><p>这个其实在前面也有提到,从根集合出发,将能访问到的对象打上标记,然后遍历堆,将没有标记的对象清除.伪代码描述如下:</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">mark_sweep() &#123;</span><br><span class="line">	<span class="comment">//标记阶段</span></span><br><span class="line">	<span class="keyword">for</span>(r : $roots) &#123;</span><br><span class="line">		mark(*r)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//清除阶段</span></span><br><span class="line">	sweeping = $heap_start</span><br><span class="line">	while(sweeping &lt; $heap_end)</span><br><span class="line">		<span class="keyword">if</span>(sweeping.mark == TRUE)</span><br><span class="line">			sweeping.mark = FALSE</span><br><span class="line">		<span class="keyword">else</span></span><br><span class="line">			sweeping.next = $free_list</span><br><span class="line">			$free_list = sweeping</span><br><span class="line">		sweeping += sweeping.size</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//标记函数</span></span><br><span class="line">mark(obj) &#123;</span><br><span class="line">	<span class="keyword">if</span>(obj.mark == FALSE) &#123;</span><br><span class="line">		obj.mark = TRUE</span><br><span class="line">		<span class="keyword">for</span>(child : children(obj))</span><br><span class="line">			mark(*child)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种方式不会挪动对象在堆中的位置,但是容易产生内存碎片.关于回收后的内存如何分配使用,可以找更相关的资料.比如参考malloc库的实现,在&lt;&lt;深入理解计算机系统&gt;&gt;一书中也有所涉及.</p>
<h4 id="引用计数法"><a href="#引用计数法" class="headerlink" title="引用计数法"></a>引用计数法</h4><p>顾名思义,在对象头记录有多少个引用了这个对象.当计数变为0的时候,我们就可以立即对这部分内存进行回收.</p>
<p>它的最大暂停时间短,但是存在多个无用对象循环引用无法释放内存的问题. Python的GC采用了引用计数法,可以去了解它如何解决循环引用的</p>
<h4 id="标记复制算法"><a href="#标记复制算法" class="headerlink" title="标记复制算法"></a>标记复制算法</h4><p>这种算法把堆划分为两部分,每次分配对象的时候只使用其中一半.当标记过程结束后,把存活的对象全都挪到堆的另一半中.</p>
<p>这种的好处是避免了内存碎片,因为每次都有一整块的空闲空间用于分配对象.但显然堆的使用效率降低了</p>
<p>==注意==</p>
<p>在这种算法中,由于挪动了对象,所以原先指向这些对象的指针,要做一个映射,使其访问到新的内存位置. 解决方法可以采用句柄的方式间接处理对象. (采用指向指针的指针也可以)</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/f6a9508f151e2be0cf45d51b3cdd0b4c.png" alt="image-20210322232131923"></p>
<p>通过引入句柄,当我们移动对象的时候,只需要修改句柄里的指针,而不用更新根的值.</p>
<h4 id="标记整理算法"><a href="#标记整理算法" class="headerlink" title="标记整理算法"></a>标记整理算法</h4><p>这种算法可以理解为先执行了标记清除算法,然后把存活的对象都挪到一起,这样空闲的空间就是连续的了.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/ebb84a415b9493c50988bfca116e4258.png" alt="image-20210322231857408"></p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/a07183d5a37a41a81b0cd6873c5caf68.png" alt="image-20210322231906519"></p>
<p>同样的,由于移动了对象,所以指针的访问也要做些修改.</p>
<h4 id="分代回收"><a href="#分代回收" class="headerlink" title="分代回收"></a>分代回收</h4><p>在对象头部分多了对象的存活年龄,据此划分了新生代,老年代对象.在堆中也是划分存储.</p>
<p>根据经验显示,新生代的对象存活率较低,所以这部分会有大量对象回收.对这部分采取标记复制算法收益比较好;</p>
<p>而针对老年代对象,则采用标记清除算法. 当需要GC时,优先从新生代入手.</p>
<p><strong>当我们对新生代进行GC时,如果有老年代的指向新生代的引用,怎么办?</strong></p>
<p>如果遍历老年代去标记,那其实又跟遍历整个堆没区别了.</p>
<p>所以这里引入了==记录集==和==写入屏障==</p>
<p>记录集上面保存了老年代对象的指针. 它是在我们更新对象指针的时候,通过写入屏障的方式加入的.</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">write_barrier(obj, field, new_obj) &#123;</span><br><span class="line">	<span class="keyword">if</span>(obj &gt;= $old_start &amp;&amp; new_obj &lt; $old_start &amp;&amp; obj.remembered == FALSE)</span><br><span class="line">		$rs[$rs_index] = obj</span><br><span class="line">		$rs_index++</span><br><span class="line">		obj.remembered = TRUE</span><br><span class="line">	*field = new_obj</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如上述伪代码那样,当老年代对象有某个属性指向新生代对象时,将其加入记录集中.</p>
<p>这样的好处是当新生代对象的内存位置移动时,能够知道要更新哪个老年代对象的指针</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/be48105e64cec299cb2218e1592616f6.png" alt="image-20210323203741382"></p>
<h4 id="增量式垃圾回收"><a href="#增量式垃圾回收" class="headerlink" title="增量式垃圾回收"></a>增量式垃圾回收</h4><p>上面提到的GC都有一个问题,它们在进行垃圾回收时会阻塞主线程,直到垃圾回收结束.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/54b247151e6342e6b1b3bc7a6f96d94b.png" alt="image-20210323204218375"></p>
<p>这里主要讲一种增量GC的算法,它的GC要分好几个阶段完成,这样保证最大暂停时间变小.</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/2f9fcb7ce4054df5db6c79d0207e56e1.png" alt="image-20210323204322491"></p>
<h5 id="三色标记算法"><a href="#三色标记算法" class="headerlink" title="三色标记算法"></a>三色标记算法</h5><p>这是Edsger W. Dijkstra等人提出的三色标记算法（Tri-color marking）,它将每个对象都标记了一个颜色:</p>
<ul>
<li><p>白色：还未搜索过的对象</p>
</li>
<li><p>灰色：正在搜索的对象</p>
</li>
<li><p>黑色：搜索完成的对象</p>
</li>
</ul>
<p>GC开始运行前所有的对象都是白色。GC一开始运行，所有从根能到达的对象都会被标记，然后被堆到栈里。GC只是发现了这样的对象，但还没有搜索完它们，所以这些对象就成了灰色对象。灰色对象会被依次从栈中取出，其子对象也会被涂成灰色。当其所有的子对象都被涂成灰色时，对象就会被涂成黑色。当GC结束时已经不存在灰色对象了，活动对象全部为黑色，垃圾则为白色。</p>
<hr>
<p>它可以分为三个阶段: 根查找阶段,标记阶段,清除阶段</p>
<p>根查找阶段把能直接从根引用的对象涂成灰色。</p>
<p>标记阶段查找灰色对象，将其子对象也涂成灰色，查找结束后将灰色对象涂成黑色。</p>
<p>清除阶段则查找堆，将白色对象连接到空闲链表，将黑色对象变回白色.</p>
<p>// 在标记,清除阶段中,其都是增量式的,即每次只标记或清除一定数量的对象后切回主线程.</p>
<hr>
<p>这里还有几个需要注意的地方.</p>
<p>问题1</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/416d59030abc0f8a58e550c3c9afe392.png" alt="image-20210323210817028"></p>
<p>如图,当A标记完所有子对象后,自己变黑. 此时切换回主线程,然后主线程的代码做了个操作,将B–C之间的引用删掉,然后加了A–C的引用.  这时再回来GC的时候,由于A已经黑了,所以不会再对其子对象做标记, 这样就会错过对象C, 这样会误回收对象!!</p>
<p>解决这个问题的办法同样使用写入屏障, 在添加A指向C的引用时, 如果C是白色的,就将其标黑.</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">write_barrier(obj, field, newobj) &#123;</span><br><span class="line">	<span class="keyword">if</span>(newobj.mark == FALSE) &#123;</span><br><span class="line">		newobj.mark = TRUE</span><br><span class="line">		push(newobj, $mark_stack)</span><br><span class="line">	&#125;</span><br><span class="line">	*field = newobj</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>问题2</p>
<p><img src="https://img-blog.csdnimg.cn/img_convert/471c98e4b5c097c01c1480de549093f1.png" alt="image-20210323211416289"></p>
<p>如图,当GC开始清除阶段时,它会把没有标黑的对象给清除掉. 如果这时切换回主线程,而我们又要分配新的对象时就要注意了, 如果分配的新对象位置在$sweeping的右边, 那我们要把新分配的对象给标成黑色的,以防被误清除.</p>
<h3 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h3><p>&lt;&lt;垃圾回收的算法与实现&gt;&gt;  —– [日]中村成洋，[日]相川光</p>
]]></content>
      <categories>
        <category>其他</category>
      </categories>
      <tags>
        <tag>垃圾回收</tag>
      </tags>
  </entry>
  <entry>
    <title>部署Tensorflow模型到服务器--方法一</title>
    <url>/2021/08/01/%E9%83%A8%E7%BD%B2Tensorflow%E6%A8%A1%E5%9E%8B%E5%88%B0%E6%9C%8D%E5%8A%A1%E5%99%A8-%E6%96%B9%E6%B3%95%E4%B8%80/</url>
    <content><![CDATA[<p><strong>基本思路：利用tensorflow官方提供的tensorflow serving进行部署，同时，为了免去环境配置等麻烦操作，可借助docker容器。</strong></p>
<h3 id="一、服务器环境选择"><a href="#一、服务器环境选择" class="headerlink" title="一、服务器环境选择"></a>一、服务器环境选择</h3><p>首先肯定要去租一个服务器，例如阿里云。一开始选了window server2012，结果很坑，装不了docker。上网想查解决方法，发现别人也遇到过这个问题。</p>
<p>了解的原因大概是：docker需要在linux的环境下运行。但通过在window server2012下使用vitural box运行linux虚拟机的办法不行，因为这样会造成二次虚拟（官方解释：阿里云给的轻量应用服务器是运行在虚拟机上的，所以不能再开虚拟机）。</p>
<p>不过网上一些大佬好像也给出了骚操作，但总之太过麻烦，不想去折腾，就没去尝试。</p>
<p>正当我觉得凉凉，想重新买linux服务器的时候，才发现阿里云的控制台上可以重新更改系统镜像，于是很愉快地换成了ubuntu18.04.</p>
<h3 id="二、Ubuntu下docker容器的安装"><a href="#二、Ubuntu下docker容器的安装" class="headerlink" title="二、Ubuntu下docker容器的安装"></a>二、Ubuntu下docker容器的安装</h3><p>前提条件：Docker 要求 Ubuntu 系统的内核版本高于 3.10 ，通过 uname -r 命令查看你当前的内核版本。</p>
<p>接下来就是在linux终端敲命令了：</p>
<p><strong>1.获取docker安装包</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wget -qO- https://get.docker.com/ | sh</span><br></pre></td></tr></table></figure>

<p>完成后会有一段提示</p>
<blockquote>
<p>If you would like to use Docker as a non-root user, you should now consider</p>
<p>  adding your user to the “docker” group with something like:</p>
<p>  sudo usermod -aG docker runoob</p>
<p>  Remember that you will have to log out and back in for this to take effect!  </p>
</blockquote>
<p>一般嫌麻烦的话，以后执行docker命令都在root下进行就可以了 </p>
<p><strong>2.运行docker容器</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo service docker start</span><br></pre></td></tr></table></figure>

<p><strong>3.测试hello-world程序</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker run hello-world</span><br></pre></td></tr></table></figure>

<p>第一次应该会失败，因为容器里还没有这个项目，所以docker会去下载，第二次运行就可以了。</p>
<h3 id="三、模型的部署"><a href="#三、模型的部署" class="headerlink" title="三、模型的部署"></a>三、模型的部署</h3><p><strong>1.拉取带tensorflow serving的docker镜像</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker pull tensorflow/serving </span><br></pre></td></tr></table></figure>

<p><strong>2.先来测试一下官方例子</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cd /root/software/</span><br><span class="line">git clone https://github.com/tensorflow/serving</span><br></pre></td></tr></table></figure>

<p>将GitHub上的TensorFlow-serving拷贝下来，里面已经有一些模型，我们通过部署一个简单的模型上docker来观察结果</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker run -p 8501:8501 \</span><br><span class="line">  --mount type=bind,\</span><br><span class="line">source=/root/software/serving/tensorflow_serving/servables/tensorflow/testdata/saved_model_half_plus_two_cpu,\</span><br><span class="line">target=/models/half_plus_two \</span><br><span class="line">  -e MODEL_NAME=half_plus_two -t tensorflow/serving &amp;</span><br></pre></td></tr></table></figure>

<p> 运行成功后，我们写一个Python的代码测试</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> json</span><br><span class="line">pdata=&#123;<span class="string">&quot;instances&quot;</span>:[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]&#125;</span><br><span class="line">param=json.dumps(pdata)</span><br><span class="line">res=requests.post(<span class="string">&#x27;http://localhost:8501/v1/models/half_plus_two:predict&#x27;</span>,data=param)</span><br><span class="line"><span class="built_in">print</span>(res.text)</span><br></pre></td></tr></table></figure>

<p> 可以看到res返回的结果，对应的是我们输入的1,2,3，之后我们就利用这种方式传递图片或其他数据过去</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    &quot;predictions&quot;: [2.5, 3.0, 3.5</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>结果分析：</p>
<p>启动docker的时候，开启了8501端口，后面url通过该端口进行访问</p>
<p>参数source表示你模型存放的文件夹，如果你去找这个文件夹，你会发现里面模型存放的格式有些特别，后面我们<strong>要部署模型时也需要先转为这种类型</strong></p>
<p>model_name是docker上模型的名称，在url上也可以看到</p>
<p>target是存放在docker上的路径</p>
<p>下面就开始部署我们自己的模型了</p>
</blockquote>
<h3 id="4-部署自己模型"><a href="#4-部署自己模型" class="headerlink" title="4.部署自己模型"></a>4.部署自己模型</h3><p>刚才第三步说过，要将模型导出为特殊的类型（有一个variables文件夹，同目录下一个pb模型，这个pb模型和之前的还不大一样）。</p>
<p>首先需要我们训练完模型后一个正常的checkpoint，转换的方法可以参考下面的做法</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">x = tf.placeholder(tf.float32, name=<span class="string">&#x27;input_x&#x27;</span>)</span><br><span class="line">feed = np.random.rand(<span class="number">100</span>)</span><br><span class="line">y = x + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">w = tf.Variable(<span class="number">0.</span>)</span><br><span class="line">b = tf.Variable(<span class="number">0.</span>)</span><br><span class="line"></span><br><span class="line">y_ = tf.add(tf.multiply(w, x), b)</span><br><span class="line"></span><br><span class="line">loss = tf.reduce_mean(tf.square(y-y_))</span><br><span class="line"></span><br><span class="line">optimizer = tf.train.GradientDescentOptimizer(<span class="number">0.2</span>)</span><br><span class="line">train = optimizer.minimize(loss)</span><br><span class="line"></span><br><span class="line">init = tf.global_variables_initializer()</span><br><span class="line"></span><br><span class="line">saver = tf.train.Saver()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess:</span><br><span class="line">    sess.run(init)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">200</span>):</span><br><span class="line">        sess.run(train, feed_dict=&#123;x: feed&#125;)</span><br><span class="line">        <span class="keyword">if</span> i % <span class="number">5</span> == <span class="number">0</span>:</span><br><span class="line">            <span class="built_in">print</span>(i, sess.run([w,b]))</span><br><span class="line"></span><br><span class="line">    saver.save(sess, <span class="string">&#x27;./linear/linear.ckpt&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.Session() <span class="keyword">as</span> sess2:</span><br><span class="line">    sess2.run(init)</span><br><span class="line">    saver.restore(sess2, <span class="string">&#x27;./linear/linear.ckpt&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将训练好的模型保存在modelName下，版本为1，当然你的版本可以随便写</span></span><br><span class="line">    builder = tf.saved_model.builder.SavedModelBuilder(<span class="string">&quot;./modelName/1&quot;</span>)</span><br><span class="line"></span><br><span class="line">    inputs = &#123;</span><br><span class="line">        <span class="comment"># 注意，这里是你预测模型的时候需要传的参数，调用模型的时候，传参必须和这里一致</span></span><br><span class="line">        <span class="comment"># 这里的input_x就是模型里面定义的输入placeholder</span></span><br><span class="line">        <span class="string">&quot;input_x&quot;</span>: tf.saved_model.utils.build_tensor_info(x)</span><br><span class="line">    &#125;</span><br><span class="line">    outputs = &#123;</span><br><span class="line">        <span class="string">&quot;output_y&quot;</span>: tf.saved_model.utils.build_tensor_info(y),</span><br><span class="line">    &#125;</span><br><span class="line">    prediction_signature = tf.saved_model.signature_def_utils.build_signature_def(</span><br><span class="line">        inputs=inputs,</span><br><span class="line">        outputs=outputs,</span><br><span class="line">        method_name=tf.saved_model.signature_constants.PREDICT_METHOD_NAME</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    builder.add_meta_graph_and_variables(</span><br><span class="line">        sess2,</span><br><span class="line">        [tf.saved_model.tag_constants.SERVING],</span><br><span class="line">        &#123;tf.saved_model.signature_constants.DEFAULT_SERVING_SIGNATURE_DEF_KEY: prediction_signature&#125;</span><br><span class="line">    )</span><br><span class="line">    builder.save()</span><br></pre></td></tr></table></figure>

<p>之后仿照样例模型，用docker运行这个模型就可以了，端口号、路径和模型名称什么的自定义即可，不冲突就行。 </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker run -p 8502:8501 \</span><br><span class="line">  --mount type=bind,\source=/root/software/serving/tensorflow_serving/servables/tensorflow/testdata/face/face2,\target=/models/face2 \</span><br><span class="line">  -e MODEL_NAME=face2 -t tensorflow/serving &amp;</span><br></pre></td></tr></table></figure>

<p> 一段测试代码，跟上面的有一点类似，具体什么参数看自己的模型</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> json</span><br><span class="line"><span class="comment"># json格式序列调整</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NumpyEncoder</span>(<span class="params">json.JSONEncoder</span>):</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">default</span>(<span class="params">self, obj</span>):</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">isinstance</span>(obj, np.ndarray):</span><br><span class="line">            <span class="keyword">return</span> obj.tolist()</span><br><span class="line">        <span class="keyword">return</span> json.JSONEncoder.default(self, obj)</span><br><span class="line">feature=np.array(<span class="built_in">range</span>(<span class="number">128</span>))</span><br><span class="line">param = &#123;</span><br><span class="line">	<span class="string">&quot;instances&quot;</span>:[</span><br><span class="line">		<span class="comment">#每一个大括号是一次请求，里面是每次请求的参数</span></span><br><span class="line">              &#123;</span><br><span class="line">		<span class="string">&quot;in&quot;</span>:feature</span><br><span class="line">	      &#125;</span><br><span class="line">	]</span><br><span class="line">&#125;</span><br><span class="line">param = json.dumps(param, cls=NumpyEncoder)</span><br><span class="line"></span><br><span class="line">res = requests.post(<span class="string">&quot;http://localhost:8502/v1/models/face2:predict&quot;</span>, data=param)</span><br><span class="line"><span class="comment"># 根据自己设定的返回数据读取</span></span><br><span class="line"><span class="comment"># softmax = json.loads(res2.text)[&quot;predictions&quot;][0]</span></span><br></pre></td></tr></table></figure>

<p>至此，大功告成。 </p>
<blockquote>
<p>PS：如果有多个模型需要部署，只需要修改本地端口号即可，不需要修改docker的端口号。（就是docker run命令的时候，只改第一个8501，不需要改第二个8501）</p>
</blockquote>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>模型部署</tag>
        <tag>Tensorflow</tag>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>部署Tensorflow模型到服务器-方法二</title>
    <url>/2021/08/01/%E9%83%A8%E7%BD%B2Tensorflow%E6%A8%A1%E5%9E%8B%E5%88%B0%E6%9C%8D%E5%8A%A1%E5%99%A8-%E6%96%B9%E6%B3%95%E4%BA%8C/</url>
    <content><![CDATA[<h3 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h3><p>python 3.8.3</p>
<p>tensorflow 2.4.0</p>
<h3 id="制作测试模型"><a href="#制作测试模型" class="headerlink" title="制作测试模型"></a>制作测试模型</h3><p><strong>注意</strong>: tensorflow版本1和版本2的接口不同, 我这里套用原来的代码, 修改部分需要兼容的代码.比如 tf.placeholder()  –&gt;  tf.compat.v1.placeholder()。如果你用的是版本1则不用修改</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line"><span class="comment"># 文件夹不存在时,会报utf-8解析错误</span></span><br><span class="line"><span class="keyword">if</span> <span class="keyword">not</span> os.path.exists(<span class="string">&#x27;./model&#x27;</span>):</span><br><span class="line">    os.mkdir(<span class="string">&#x27;./model&#x27;</span>)</span><br><span class="line"></span><br><span class="line">tf.compat.v1.disable_eager_execution()</span><br><span class="line"></span><br><span class="line">x = tf.compat.v1.placeholder(tf.float32, name=<span class="string">&#x27;inputX&#x27;</span>)</span><br><span class="line">feed = np.random.rand(<span class="number">100</span>)</span><br><span class="line">y = <span class="number">2</span> * x + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">w = tf.Variable(<span class="number">0.</span>)</span><br><span class="line">b = tf.Variable(<span class="number">0.</span>)</span><br><span class="line"></span><br><span class="line">y_ = tf.add(tf.multiply(w, x), b, name=<span class="string">&#x27;outputY&#x27;</span>)</span><br><span class="line"></span><br><span class="line">loss = tf.reduce_mean(tf.square(y - y_))</span><br><span class="line"></span><br><span class="line">optimizer = tf.compat.v1.train.GradientDescentOptimizer(<span class="number">0.2</span>)</span><br><span class="line">train = optimizer.minimize(loss)</span><br><span class="line"></span><br><span class="line">init = tf.compat.v1.global_variables_initializer()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.compat.v1.Session() <span class="keyword">as</span> sess:</span><br><span class="line">    sess.run(init)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">200</span>):</span><br><span class="line">        sess.run(train, feed_dict=&#123;x: feed&#125;)</span><br><span class="line">        <span class="keyword">if</span> i % <span class="number">5</span> == <span class="number">0</span>:</span><br><span class="line">            <span class="built_in">print</span>(i, sess.run([w, b]))</span><br><span class="line"></span><br><span class="line">    constant_graph = tf.compat.v1.graph_util.convert_variables_to_constants(sess, sess.graph_def, [<span class="string">&#x27;outputY&#x27;</span>])</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.compat.v1.gfile.FastGFile(<span class="string">&#x27;./model/linear.pb&#x27;</span>, mode=<span class="string">&#x27;wb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">    f.write(constant_graph.SerializeToString())</span><br></pre></td></tr></table></figure>

<h3 id="部署上线"><a href="#部署上线" class="headerlink" title="部署上线"></a>部署上线</h3><p>这里使用python的web服务器框架Flask，很容易上手的，而且Pycharm在创建项目时就能选择。<br><img src="https://img-blog.csdnimg.cn/20201217181847852.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzE3MTkwMTIx,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"></p>
<p>这里直接贴代码</p>
<h5 id="app-py"><a href="#app-py" class="headerlink" title="app.py"></a>app.py</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> request</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> tf.compat.v1.gfile.FastGFile(<span class="string">&#x27;./model/linear.pb&#x27;</span>, <span class="string">&#x27;rb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">    graph_def = tf.compat.v1.GraphDef()</span><br><span class="line">    graph_def.ParseFromString(f.read())</span><br><span class="line">    tf.import_graph_def(graph_def, name=<span class="string">&#x27;&#x27;</span>)</span><br><span class="line">sess = tf.compat.v1.Session()</span><br><span class="line">output = sess.graph.get_tensor_by_name(<span class="string">&#x27;outputY:0&#x27;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route(<span class="params"><span class="string">&#x27;/predict&#x27;</span>, methods=[<span class="string">&quot;GET&quot;</span>]</span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">testPredict</span>():</span></span><br><span class="line">    inputX = request.args.get(<span class="string">&quot;inputX&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">str</span>(sess.run(output, feed_dict=&#123;<span class="string">&#x27;inputX:0&#x27;</span>: inputX&#125;))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    app.run()</span><br></pre></td></tr></table></figure>

<h3 id="验证结果"><a href="#验证结果" class="headerlink" title="验证结果"></a>验证结果</h3><p>程序启动后，默认监听的是5000端口，我们用浏览器访问 <a href="http://localhost:5000/predict?inputX=2%EF%BC%8C%E5%8F%AF%E4%BB%A5%E7%9C%8B%E5%88%B0%E7%BB%93%E6%9E%9C">http://localhost:5000/predict?inputX=2，可以看到结果</a></p>
<p><img src="https://img-blog.csdnimg.cn/2020121718193840.png#pic_center" alt="在这里插入图片描述"></p>
<p>完结撒花❀❀❀</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>模型部署</tag>
        <tag>Tensorflow</tag>
      </tags>
  </entry>
  <entry>
    <title>自己手动生成二维码</title>
    <url>/2021/08/02/%E8%87%AA%E5%B7%B1%E6%89%8B%E5%8A%A8%E7%94%9F%E6%88%90%E4%BA%8C%E7%BB%B4%E7%A0%81/</url>
    <content><![CDATA[<p>虽然生成二维码不是什么难事，毕竟有那么多轮子，工作中你就是调一下SDK，并不会去关心它具体怎么实现的（比如zxing算一个，不想麻烦的可以用这个）。但是两三行调用别人的代码就完事感觉没什么意思，<del>其实还是想折腾</del>。</p>
<ul>
<li>如果对二维码的原理不熟悉，可以参考我转载的这篇博客<a href="https://blog.csdn.net/qq_17190121/article/details/104715136">https://blog.csdn.net/qq_17190121/article/details/104715136</a><br>博主写得很清楚了，看一两遍然后就懂了</li>
<li>找不到官方文档，貌似ISO要收费。。然后就在百度文库中找了中文版的<a href="https://wenku.baidu.com/view/ef77275f312b3169a451a4a4.html?pn=50">https://wenku.baidu.com/view/ef77275f312b3169a451a4a4.html?pn=50</a></li>
</ul>
<h5 id="自我感觉代码写得一般，但逻辑很清晰，基本步骤如图"><a href="#自我感觉代码写得一般，但逻辑很清晰，基本步骤如图" class="headerlink" title="自我感觉代码写得一般，但逻辑很清晰，基本步骤如图"></a>自我感觉代码写得一般，但逻辑很清晰，基本步骤如图<img src="https://img-blog.csdnimg.cn/20200307162618411.gif" alt="在这里插入图片描述"></h5><blockquote>
<p>其实上面这个图是在写代码出现bug时想到的一个办法，嗯…，还挺好看的</p>
</blockquote>
<h5 id="因为目的是用手机扫码，所以二维码的规格不用特别大，一般只需要存一个网站的地址即可，微信-支付宝扫到二维码时会自动跳转到这个网址。"><a href="#因为目的是用手机扫码，所以二维码的规格不用特别大，一般只需要存一个网站的地址即可，微信-支付宝扫到二维码时会自动跳转到这个网址。" class="headerlink" title="因为目的是用手机扫码，所以二维码的规格不用特别大，一般只需要存一个网站的地址即可，微信/支付宝扫到二维码时会自动跳转到这个网址。"></a>因为目的是用手机扫码，所以二维码的规格不用特别大，一般只需要存一个网站的地址即可，微信/支付宝扫到二维码时会自动跳转到这个网址。</h5><h6 id="我这里用的规格"><a href="#我这里用的规格" class="headerlink" title="我这里用的规格"></a>我这里用的规格</h6><table>
<thead>
<tr>
<th>版本</th>
<th>纠错等级</th>
<th>编码方式</th>
</tr>
</thead>
<tbody><tr>
<td>6</td>
<td>H</td>
<td>Byte Mode</td>
</tr>
</tbody></table>
<h4 id="参考文档位置"><a href="#参考文档位置" class="headerlink" title="参考文档位置"></a>参考文档位置</h4><p><img src="https://img-blog.csdnimg.cn/20200307162827574.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzE3MTkwMTIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><img src="https://img-blog.csdnimg.cn/2020030716283698.png" alt="在这里插入图片描述"><br><img src="https://img-blog.csdnimg.cn/20200307162840189.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzE3MTkwMTIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br><img src="https://img-blog.csdnimg.cn/20200307162844555.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzE3MTkwMTIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h3 id="初始化数据"><a href="#初始化数据" class="headerlink" title="初始化数据"></a>初始化数据</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 相关信息</span></span><br><span class="line">self.version = <span class="number">6</span></span><br><span class="line"><span class="comment"># H纠错等级的指示码，版本6的字节数，划分数据块数，每块数据数，每块纠错数</span></span><br><span class="line">self.relate_info = &#123;<span class="string">&#x27;err_code&#x27;</span>: <span class="string">&#x27;10&#x27;</span>, <span class="string">&#x27;total_bytes&#x27;</span>: <span class="number">172</span>, <span class="string">&#x27;number_of_block&#x27;</span>: <span class="number">4</span>,</span><br><span class="line">                    <span class="string">&#x27;per_block_data&#x27;</span>: <span class="number">15</span>, <span class="string">&#x27;per_block_err&#x27;</span>: <span class="number">28</span>&#125;</span><br><span class="line"><span class="comment"># 对齐图案中心点位置</span></span><br><span class="line">self.align_pos = <span class="number">34</span></span><br><span class="line">self.size = <span class="number">21</span> + (self.version - <span class="number">1</span>) * <span class="number">4</span></span><br><span class="line"><span class="comment"># 0表示白，1表示黑，2表示未使用</span></span><br><span class="line">self.pattern = np.ones([self.size, self.size], dtype=<span class="built_in">int</span>) * <span class="number">2</span></span><br><span class="line">self.data = data</span><br><span class="line"><span class="comment"># 格式：&#123;&#x27;000&#x27;:[[1,0,0,1]...[0,1,1,0],&#x27;001&#x27;:...&#125;</span></span><br><span class="line">self.masking_pattern = <span class="literal">None</span></span><br><span class="line">self.masking_coding = <span class="literal">None</span></span><br><span class="line">self.draw = <span class="literal">True</span></span><br></pre></td></tr></table></figure>

<h3 id="绘制定位图案"><a href="#绘制定位图案" class="headerlink" title="绘制定位图案"></a>绘制定位图案</h3><p>版本确定了，位置也就确定了</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_position_pattern</span>(<span class="params">self</span>):</span></span><br><span class="line">    <span class="comment"># 画好一个定位图案</span></span><br><span class="line">    position_pattern = np.ones([<span class="number">7</span>, <span class="number">7</span>], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">6</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">6</span>):</span><br><span class="line">            <span class="keyword">if</span> i == <span class="number">1</span> <span class="keyword">or</span> i == <span class="number">5</span>:</span><br><span class="line">                position_pattern[i][j] = <span class="number">0</span></span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            <span class="keyword">if</span> j == <span class="number">1</span> <span class="keyword">or</span> j == <span class="number">5</span>:</span><br><span class="line">                position_pattern[i][j] = <span class="number">0</span></span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">    <span class="comment"># 放在三个位置</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">7</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">7</span>):</span><br><span class="line">            self.pattern[i][j] = position_pattern[i][j]</span><br><span class="line">            self.pattern[i][j + self.size - <span class="number">7</span>] = position_pattern[i][j]</span><br><span class="line">            self.pattern[i + self.size - <span class="number">7</span>][j] = position_pattern[i][j]</span><br><span class="line">            <span class="keyword">if</span> self.draw:</span><br><span class="line">                self.dynamic_draw()</span><br></pre></td></tr></table></figure>

<h3 id="绘制定位图案白边"><a href="#绘制定位图案白边" class="headerlink" title="绘制定位图案白边"></a>绘制定位图案白边</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_position_round_pattern</span>(<span class="params">self</span>):</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">8</span>):</span><br><span class="line">        self.pattern[i][<span class="number">7</span>] = <span class="number">0</span></span><br><span class="line">        self.pattern[<span class="number">7</span>][i] = <span class="number">0</span></span><br><span class="line">        self.pattern[i][self.size - <span class="number">8</span>] = <span class="number">0</span></span><br><span class="line">        self.pattern[self.size - <span class="number">8</span>][i] = <span class="number">0</span></span><br><span class="line">        self.pattern[i + self.size - <span class="number">8</span>][<span class="number">7</span>] = <span class="number">0</span></span><br><span class="line">        self.pattern[<span class="number">7</span>][i + self.size - <span class="number">8</span>] = <span class="number">0</span></span><br><span class="line">        <span class="keyword">if</span> self.draw:</span><br><span class="line">            self.dynamic_draw()</span><br></pre></td></tr></table></figure>

<h3 id="绘制对齐图案"><a href="#绘制对齐图案" class="headerlink" title="绘制对齐图案"></a>绘制对齐图案</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_alignment_pattern</span>(<span class="params">self</span>):</span></span><br><span class="line">    <span class="comment"># 画好一个对齐图案</span></span><br><span class="line">    alignment_pattern = np.ones([<span class="number">5</span>, <span class="number">5</span>], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">4</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">4</span>):</span><br><span class="line">            <span class="keyword">if</span> i == <span class="number">1</span> <span class="keyword">or</span> i == <span class="number">3</span>:</span><br><span class="line">                alignment_pattern[i][j] = <span class="number">0</span></span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            <span class="keyword">if</span> j == <span class="number">1</span> <span class="keyword">or</span> j == <span class="number">3</span>:</span><br><span class="line">                alignment_pattern[i][j] = <span class="number">0</span></span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">    <span class="comment"># 只放置在一个位置</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">5</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">5</span>):</span><br><span class="line">            self.pattern[i + self.align_pos - <span class="number">2</span>][j + self.align_pos - <span class="number">2</span>] = alignment_pattern[i][j]</span><br><span class="line">            <span class="keyword">if</span> self.draw:</span><br><span class="line">                self.dynamic_draw()</span><br></pre></td></tr></table></figure>

<h3 id="绘制时序图案"><a href="#绘制时序图案" class="headerlink" title="绘制时序图案"></a>绘制时序图案</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_timing_pattern</span>(<span class="params">self</span>):</span></span><br><span class="line">    fill_black = <span class="literal">True</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">8</span>, self.size - <span class="number">7</span>):</span><br><span class="line">        <span class="keyword">if</span> fill_black:</span><br><span class="line">            self.pattern[<span class="number">6</span>][i] = <span class="number">1</span></span><br><span class="line">            self.pattern[i][<span class="number">6</span>] = <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.pattern[<span class="number">6</span>][i] = <span class="number">0</span></span><br><span class="line">            self.pattern[i][<span class="number">6</span>] = <span class="number">0</span></span><br><span class="line">        fill_black = <span class="keyword">not</span> fill_black</span><br><span class="line">        <span class="keyword">if</span> self.draw:</span><br><span class="line">            self.dynamic_draw()</span><br></pre></td></tr></table></figure>

<h3 id="绘制临时的格式图案"><a href="#绘制临时的格式图案" class="headerlink" title="绘制临时的格式图案"></a>绘制临时的格式图案</h3><p>主要是为了占坑，让后面填充数据的操作更简单</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_tmp_format_pattern</span>(<span class="params">self</span>):</span></span><br><span class="line">    <span class="comment"># 右上角0-7</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">8</span>):</span><br><span class="line">    	self.pattern[<span class="number">8</span>][self.size - <span class="number">1</span> - i] = <span class="number">0</span></span><br><span class="line">    <span class="comment"># 左下角8-14</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">7</span>):</span><br><span class="line">        self.pattern[self.size - <span class="number">7</span> + i][<span class="number">8</span>] = <span class="number">0</span></span><br><span class="line">    <span class="comment"># 固定黑点</span></span><br><span class="line">        self.pattern[self.size - <span class="number">8</span>][<span class="number">8</span>] = <span class="number">0</span></span><br><span class="line">    <span class="comment"># 左上角部分</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">6</span>):</span><br><span class="line">        self.pattern[i][<span class="number">8</span>] = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">6</span>):</span><br><span class="line">        self.pattern[<span class="number">8</span>][<span class="number">5</span> - i] = <span class="number">0</span></span><br><span class="line">        <span class="comment"># 位置是固定的</span></span><br><span class="line">        self.pattern[<span class="number">7</span>][<span class="number">8</span>] = <span class="number">0</span></span><br><span class="line">        self.pattern[<span class="number">8</span>][<span class="number">8</span>] = <span class="number">0</span></span><br><span class="line">        self.pattern[<span class="number">8</span>][<span class="number">7</span>] = <span class="number">0</span></span><br></pre></td></tr></table></figure>

<h3 id="绘制版本图案"><a href="#绘制版本图案" class="headerlink" title="绘制版本图案"></a>绘制版本图案</h3><p>这个版本刚好不用，版本7以上才需要</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_version_pattern</span>(<span class="params">self</span>):</span></span><br><span class="line">    <span class="comment"># version7及以上才有</span></span><br><span class="line">    <span class="keyword">pass</span></span><br></pre></td></tr></table></figure>

<h3 id="获取掩膜图案"><a href="#获取掩膜图案" class="headerlink" title="获取掩膜图案"></a>获取掩膜图案</h3><p>这个版本是长这样的</p>
<p><img src="https://img-blog.csdnimg.cn/20200307162944343.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzE3MTkwMTIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>看看如何生成这么奇怪的形状</p>
<p><img src="https://img-blog.csdnimg.cn/20200307162951328.png" alt="在这里插入图片描述"></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_masking_area</span>(<span class="params">self</span>):</span></span><br><span class="line">    masking_area = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="keyword">if</span> self.pattern[i][j] == <span class="number">2</span>:</span><br><span class="line">                masking_area[i][j] = <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> masking_area</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_masking_pattern</span>(<span class="params">self</span>):</span></span><br><span class="line">    <span class="comment"># 八个模板</span></span><br><span class="line">    masking_array = &#123;&#125;</span><br><span class="line">    masking_area = self.get_masking_area()</span><br><span class="line">    <span class="comment">##########</span></span><br><span class="line">    code = <span class="string">&quot;000&quot;</span></span><br><span class="line">    masking = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="keyword">if</span> (i + j) % <span class="number">2</span> == <span class="number">0</span>:</span><br><span class="line">                masking[i][j] = masking_area[i][j]</span><br><span class="line">    masking_array[code] = masking</span><br><span class="line">    <span class="comment">##########</span></span><br><span class="line">    code = <span class="string">&quot;001&quot;</span></span><br><span class="line">    masking = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="keyword">if</span> i % <span class="number">2</span> == <span class="number">0</span>:</span><br><span class="line">                masking[i][j] = masking_area[i][j]</span><br><span class="line">    masking_array[code] = masking</span><br><span class="line">    <span class="comment">##########</span></span><br><span class="line">    code = <span class="string">&quot;010&quot;</span></span><br><span class="line">    masking = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="keyword">if</span> j % <span class="number">3</span> == <span class="number">0</span>:</span><br><span class="line">                masking[i][j] = masking_area[i][j]</span><br><span class="line">    masking_array[code] = masking</span><br><span class="line">    <span class="comment">##########</span></span><br><span class="line">    code = <span class="string">&quot;011&quot;</span></span><br><span class="line">    masking = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="keyword">if</span> (i + j) % <span class="number">3</span> == <span class="number">0</span>:</span><br><span class="line">                masking[i][j] = masking_area[i][j]</span><br><span class="line">    masking_array[code] = masking</span><br><span class="line">    <span class="comment">##########</span></span><br><span class="line">    code = <span class="string">&quot;100&quot;</span></span><br><span class="line">    masking = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="comment"># 这里有一点坑,不转为int会变成float+float</span></span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">int</span>(i / <span class="number">2</span>) + <span class="built_in">int</span>(j / <span class="number">3</span>)) % <span class="number">2</span> == <span class="number">0</span>:</span><br><span class="line">                masking[i][j] = masking_area[i][j]</span><br><span class="line">    masking_array[code] = masking</span><br><span class="line">    <span class="comment">##########</span></span><br><span class="line">    code = <span class="string">&quot;101&quot;</span></span><br><span class="line">    masking = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="keyword">if</span> ((i * j) % <span class="number">2</span> + (i * j) % <span class="number">3</span>) == <span class="number">0</span>:</span><br><span class="line">                masking[i][j] = masking_area[i][j]</span><br><span class="line">    masking_array[code] = masking</span><br><span class="line">    <span class="comment">##########</span></span><br><span class="line">    code = <span class="string">&quot;110&quot;</span></span><br><span class="line">    masking = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="keyword">if</span> ((i * j) % <span class="number">2</span> + (i * j) % <span class="number">3</span>) % <span class="number">2</span> == <span class="number">0</span>:</span><br><span class="line">                masking[i][j] = masking_area[i][j]</span><br><span class="line">    masking_array[code] = masking</span><br><span class="line">    <span class="comment">##########</span></span><br><span class="line">    code = <span class="string">&quot;111&quot;</span></span><br><span class="line">    masking = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="keyword">if</span> ((i * j) % <span class="number">3</span> + (i + j) % <span class="number">2</span>) % <span class="number">2</span> == <span class="number">0</span>:</span><br><span class="line">                masking[i][j] = masking_area[i][j]</span><br><span class="line">    masking_array[code] = masking</span><br><span class="line"></span><br><span class="line">    self.masking_pattern = masking_array</span><br></pre></td></tr></table></figure>

<h3 id="填充最初的数据"><a href="#填充最初的数据" class="headerlink" title="填充最初的数据"></a>填充最初的数据</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fill_data</span>(<span class="params">self</span>):</span></span><br><span class="line">    <span class="comment"># 右下角开始填充，先右后左，蛇形走位</span></span><br><span class="line">    x = self.size - <span class="number">1</span></span><br><span class="line">    y = self.size - <span class="number">1</span></span><br><span class="line">    pos_right = <span class="literal">True</span></span><br><span class="line">    direct = <span class="number">1</span>  <span class="comment"># 0向下  1向上</span></span><br><span class="line">    index = <span class="number">0</span></span><br><span class="line">    data = _rsEncode(self.data, self.relate_info[<span class="string">&#x27;per_block_data&#x27;</span>],</span><br><span class="line">                     self.relate_info[<span class="string">&#x27;number_of_block&#x27;</span>], self.relate_info[<span class="string">&#x27;per_block_err&#x27;</span>])</span><br><span class="line">    remainder = <span class="number">7</span></span><br><span class="line">    <span class="keyword">while</span> index &lt; <span class="built_in">len</span>(data) + remainder:</span><br><span class="line">        <span class="keyword">if</span> self.pattern[x][y] == <span class="number">2</span>:</span><br><span class="line">            <span class="keyword">if</span> index &lt; <span class="built_in">len</span>(data):</span><br><span class="line">                self.pattern[x][y] = <span class="built_in">int</span>(data[index])</span><br><span class="line">            <span class="comment"># 把剩下7个空白补上--版本6剩下7个空白位置</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                self.pattern[x][y] = <span class="number">0</span></span><br><span class="line">            index = index + <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> pos_right:</span><br><span class="line">            y = y - <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            x = x + (-<span class="number">1</span>) ** direct</span><br><span class="line">            y = y + <span class="number">1</span></span><br><span class="line">        pos_right = <span class="keyword">not</span> pos_right</span><br><span class="line">        <span class="comment"># 向上飞出天了</span></span><br><span class="line">        <span class="keyword">if</span> x &lt; <span class="number">0</span>:</span><br><span class="line">            direct = <span class="number">0</span></span><br><span class="line">            x = <span class="number">0</span></span><br><span class="line">            y = y - <span class="number">2</span></span><br><span class="line">            <span class="comment"># 刚好碰到时序线,这个的位置固定y为6，要左移一格</span></span><br><span class="line">            <span class="keyword">if</span> y == <span class="number">6</span>:</span><br><span class="line">                y = y - <span class="number">1</span></span><br><span class="line">        <span class="comment"># 向下钻出地了</span></span><br><span class="line">        <span class="keyword">if</span> x &gt; <span class="number">40</span>:</span><br><span class="line">            direct = <span class="number">1</span></span><br><span class="line">            x = <span class="number">40</span></span><br><span class="line">            y = y - <span class="number">2</span></span><br><span class="line">        <span class="keyword">if</span> self.draw:</span><br><span class="line">            self.dynamic_draw()</span><br></pre></td></tr></table></figure>

<h3 id="与所有掩膜做异或，选择处罚最低的"><a href="#与所有掩膜做异或，选择处罚最低的" class="headerlink" title="与所有掩膜做异或，选择处罚最低的"></a>与所有掩膜做异或，选择处罚最低的</h3><p>这里的评价一开始代码写的很晕，因为中文文档的表述不是很清楚。所以我找了英文的来看，它下面有详细的解释，理解起来容易很多</p>
<p><img src="https://img-blog.csdnimg.cn/20200307163013534.png" alt="在这里插入图片描述"></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">find_best_masking_and_set</span>(<span class="params">self</span>):</span></span><br><span class="line">    coding = <span class="string">&#x27;&#x27;</span></span><br><span class="line">    score = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> key <span class="keyword">in</span> self.masking_pattern.keys():</span><br><span class="line">        mask_score = self.get_mask_score(self.masking_pattern[key], key)</span><br><span class="line">        <span class="built_in">print</span>(key, mask_score)</span><br><span class="line">        <span class="keyword">if</span> coding == <span class="string">&#x27;&#x27;</span>:</span><br><span class="line">            coding = key</span><br><span class="line">            score = mask_score</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">if</span> score &gt; mask_score:</span><br><span class="line">                score = mask_score</span><br><span class="line">                coding = key</span><br><span class="line">    self.masking_coding = coding</span><br><span class="line">    <span class="comment"># set</span></span><br><span class="line">    <span class="comment"># 可以针对去调整评价算法</span></span><br><span class="line">    masking_pattern = self.masking_pattern[coding]</span><br><span class="line">    <span class="comment"># 执行异或操作</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            self.pattern[i][j] = masking_pattern[i][j] ^ self.pattern[i][j]</span><br><span class="line">            <span class="keyword">if</span> self.draw:</span><br><span class="line">                self.dynamic_draw()</span><br><span class="line">    self.get_format_pattern()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_mask_score</span>(<span class="params">self, masking_pattern, key</span>):</span></span><br><span class="line">    array = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    self.masking_coding = key</span><br><span class="line">    self.get_format_pattern()</span><br><span class="line">    <span class="comment"># 执行异或操作</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            array[i][j] = masking_pattern[i][j] ^ self.pattern[i][j]</span><br><span class="line"></span><br><span class="line">    n1 = <span class="number">0</span></span><br><span class="line">    n2 = <span class="number">0</span></span><br><span class="line">    n3 = <span class="number">0</span></span><br><span class="line">    n4 = <span class="number">0</span></span><br><span class="line">    <span class="comment"># 横竖存黑白相间次数的数组</span></span><br><span class="line">    row_interval = []</span><br><span class="line">    col_interval = []</span><br><span class="line">    <span class="comment"># 横竖连续5个以上颜色相同，分数：个数-2</span></span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> i &lt; self.size:</span><br><span class="line">        j = <span class="number">0</span></span><br><span class="line">        interval = []</span><br><span class="line">        <span class="keyword">while</span> j &lt; self.size:</span><br><span class="line">            times = <span class="number">0</span></span><br><span class="line">            k = j</span><br><span class="line">            <span class="keyword">while</span> k &lt; self.size <span class="keyword">and</span> array[i][k] == array[i][j]:</span><br><span class="line">                k = k + <span class="number">1</span></span><br><span class="line">                times = times + <span class="number">1</span></span><br><span class="line">            interval.append(times)</span><br><span class="line">            <span class="keyword">if</span> times &gt;= <span class="number">5</span>:</span><br><span class="line">                n1 = n1 + times - <span class="number">2</span></span><br><span class="line">            j = k</span><br><span class="line">        i = i + <span class="number">1</span></span><br><span class="line">        row_interval.append(interval)</span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    <span class="keyword">while</span> i &lt; self.size:</span><br><span class="line">        j = <span class="number">0</span></span><br><span class="line">        interval = []</span><br><span class="line">        <span class="keyword">while</span> j &lt; self.size:</span><br><span class="line">            times = <span class="number">0</span></span><br><span class="line">            k = j</span><br><span class="line">            <span class="keyword">while</span> k &lt; self.size <span class="keyword">and</span> array[k][i] == array[j][i]:</span><br><span class="line">                k = k + <span class="number">1</span></span><br><span class="line">                times = times + <span class="number">1</span></span><br><span class="line">            interval.append(times)</span><br><span class="line">            <span class="keyword">if</span> times &gt;= <span class="number">5</span>:</span><br><span class="line">                n1 = n1 + times - <span class="number">2</span></span><br><span class="line">            j = k</span><br><span class="line">        i = i + <span class="number">1</span></span><br><span class="line">        col_interval.append(interval)</span><br><span class="line">    <span class="comment"># 存在2X2颜色相同的块</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size - <span class="number">1</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size - <span class="number">1</span>):</span><br><span class="line">            <span class="keyword">if</span> array[i][j] == array[i + <span class="number">1</span>][j] <span class="keyword">and</span> array[i][j] == array[i][j + <span class="number">1</span>]:</span><br><span class="line">                <span class="keyword">if</span> array[i][j] == array[i + <span class="number">1</span>][j + <span class="number">1</span>]:</span><br><span class="line">                    n2 = n2 + <span class="number">3</span></span><br><span class="line">    <span class="comment"># 横竖出现1:1:3:1:1 的黑白黑白黑，且前面或后面有4个以上白</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">if</span> array[i][<span class="number">0</span>] == <span class="number">1</span>:</span><br><span class="line">            first_dark = <span class="literal">True</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            first_dark = <span class="literal">False</span></span><br><span class="line">        <span class="comment"># bug warning</span></span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="built_in">len</span>(row_interval[i])-<span class="number">5</span>):</span><br><span class="line">            <span class="keyword">if</span> row_interval[i][j] == row_interval[i][j+<span class="number">1</span>] <span class="keyword">and</span> row_interval[i][j]*<span class="number">3</span> == row_interval[i][j+<span class="number">2</span>]:</span><br><span class="line">                <span class="keyword">if</span> row_interval[i][j] == row_interval[i][j+<span class="number">3</span>] <span class="keyword">and</span> row_interval[i][j] == row_interval[i][j+<span class="number">4</span>]:</span><br><span class="line">                    <span class="keyword">if</span> row_interval[i][j-<span class="number">1</span>] &gt;= <span class="number">4</span> <span class="keyword">and</span> <span class="keyword">not</span> first_dark:</span><br><span class="line">                        n3 = n3 + <span class="number">40</span></span><br><span class="line">                        first_dark = <span class="keyword">not</span> first_dark</span><br><span class="line">                        <span class="keyword">continue</span></span><br><span class="line">                    <span class="keyword">if</span> row_interval[i][j+<span class="number">5</span>] &gt;= <span class="number">4</span> <span class="keyword">and</span> <span class="keyword">not</span> first_dark:</span><br><span class="line">                        n3 = n3 + <span class="number">40</span></span><br><span class="line">                        first_dark = <span class="keyword">not</span> first_dark</span><br><span class="line">                        <span class="keyword">continue</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">if</span> array[<span class="number">0</span>][i] == <span class="number">1</span>:</span><br><span class="line">            first_dark = <span class="literal">True</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            first_dark = <span class="literal">False</span></span><br><span class="line">        <span class="comment"># bug warning</span></span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="built_in">len</span>(col_interval[i])-<span class="number">5</span>):</span><br><span class="line">            <span class="keyword">if</span> col_interval[i][j] == col_interval[i][j+<span class="number">1</span>] <span class="keyword">and</span> col_interval[i][j]*<span class="number">3</span> == col_interval[i][j+<span class="number">2</span>]:</span><br><span class="line">                <span class="keyword">if</span> col_interval[i][j] == col_interval[i][j+<span class="number">3</span>] <span class="keyword">and</span> col_interval[i][j] == col_interval[i][j+<span class="number">4</span>]:</span><br><span class="line">                    <span class="keyword">if</span> col_interval[i][j-<span class="number">1</span>] &gt;= <span class="number">4</span> <span class="keyword">and</span> <span class="keyword">not</span> first_dark:</span><br><span class="line">                        n3 = n3 + <span class="number">40</span></span><br><span class="line">                        first_dark = <span class="keyword">not</span> first_dark</span><br><span class="line">                        <span class="keyword">continue</span></span><br><span class="line">                    <span class="keyword">if</span> col_interval[i][j+<span class="number">5</span>] &gt;= <span class="number">4</span> <span class="keyword">and</span> <span class="keyword">not</span> first_dark:</span><br><span class="line">                        n3 = n3 + <span class="number">40</span></span><br><span class="line">                        first_dark = <span class="keyword">not</span> first_dark</span><br><span class="line">                        <span class="keyword">continue</span></span><br><span class="line">    <span class="comment"># 黑色块的数量</span></span><br><span class="line">    dark_num = array.<span class="built_in">sum</span>()</span><br><span class="line">    dark_rate = dark_num * <span class="number">100</span> / (self.size * self.size)</span><br><span class="line">    n4 = n4 + <span class="built_in">int</span>(math.fabs(dark_rate - <span class="number">50</span>) / <span class="number">5</span>) * <span class="number">10</span></span><br><span class="line">    <span class="keyword">return</span> n1 + n2 + n3 + n4</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_format_pattern</span>(<span class="params">self</span>):</span></span><br><span class="line">    code = <span class="built_in">int</span>(self.relate_info[<span class="string">&#x27;err_code&#x27;</span>] + self.masking_coding, <span class="number">2</span>)</span><br><span class="line">    format_code = <span class="string">&#x27;&#123;:015b&#125;&#x27;</span>.<span class="built_in">format</span>(_fmtEncode(code))</span><br><span class="line">    <span class="comment"># 默认高位在前，反转一下</span></span><br><span class="line">    format_code = format_code[::-<span class="number">1</span>]</span><br><span class="line">    <span class="comment"># 右上角0-7</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">8</span>):</span><br><span class="line">        self.pattern[<span class="number">8</span>][self.size - <span class="number">1</span> - i] = <span class="built_in">int</span>(format_code[i])</span><br><span class="line">    <span class="comment"># 左下角8-14</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">7</span>):</span><br><span class="line">        self.pattern[self.size - <span class="number">7</span> + i][<span class="number">8</span>] = <span class="built_in">int</span>(format_code[i + <span class="number">8</span>])</span><br><span class="line">    <span class="comment"># 固定黑点</span></span><br><span class="line">    self.pattern[self.size - <span class="number">8</span>][<span class="number">8</span>] = <span class="number">1</span></span><br><span class="line">    <span class="comment"># 左上角部分</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">6</span>):</span><br><span class="line">        self.pattern[i][<span class="number">8</span>] = <span class="built_in">int</span>(format_code[i])</span><br><span class="line">        self.pattern[<span class="number">8</span>][<span class="number">5</span> - i] = <span class="built_in">int</span>(format_code[<span class="number">9</span> + i])</span><br><span class="line">    <span class="comment"># 位置是固定的</span></span><br><span class="line">    self.pattern[<span class="number">7</span>][<span class="number">8</span>] = <span class="built_in">int</span>(format_code[<span class="number">6</span>])</span><br><span class="line">    self.pattern[<span class="number">8</span>][<span class="number">8</span>] = <span class="built_in">int</span>(format_code[<span class="number">7</span>])</span><br><span class="line">    self.pattern[<span class="number">8</span>][<span class="number">7</span>] = <span class="built_in">int</span>(format_code[<span class="number">8</span>])</span><br></pre></td></tr></table></figure>

<h3 id="最后调用画图，看看自己做的二维码"><a href="#最后调用画图，看看自己做的二维码" class="headerlink" title="最后调用画图，看看自己做的二维码"></a>最后调用画图，看看自己做的二维码</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">dynamic_draw</span>(<span class="params">self</span>):</span></span><br><span class="line">    self.show()</span><br><span class="line">    plt.pause(<span class="number">0.001</span>)</span><br><span class="line">    plt.clf()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">show</span>(<span class="params">self</span>):</span></span><br><span class="line">    pic = np.zeros([self.size, self.size], dtype=<span class="built_in">int</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(self.size):</span><br><span class="line">            <span class="keyword">if</span> self.pattern[i][j] == <span class="number">1</span>:</span><br><span class="line">                pic[i][j] = <span class="number">0</span></span><br><span class="line">            <span class="keyword">elif</span> self.pattern[i][j] == <span class="number">0</span>:</span><br><span class="line">                pic[i][j] = <span class="number">255</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                pic[i][j] = <span class="number">200</span></span><br><span class="line">    plt.imshow(pic, cmap=<span class="string">&quot;gray&quot;</span>)</span><br><span class="line">    plt.axis(<span class="string">&#x27;off&#x27;</span>)</span><br><span class="line">    plt.show()</span><br></pre></td></tr></table></figure>
<p><img src="https://img-blog.csdnimg.cn/20200307163205516.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzE3MTkwMTIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<blockquote>
<p>logo是我后期加上去的。。。</p>
</blockquote>
<h3 id="关于其中涉及到的两种编码"><a href="#关于其中涉及到的两种编码" class="headerlink" title="关于其中涉及到的两种编码"></a>关于其中涉及到的两种编码</h3><ul>
<li><h5 id="编数据的用到的里德-所罗门码：这里用了第三方库，虽然网上也能找到源代码，但有几十行，我就不添加了，直接第三方工具，一两行代码搞定"><a href="#编数据的用到的里德-所罗门码：这里用了第三方库，虽然网上也能找到源代码，但有几十行，我就不添加了，直接第三方工具，一两行代码搞定" class="headerlink" title="编数据的用到的里德-所罗门码：这里用了第三方库，虽然网上也能找到源代码，但有几十行，我就不添加了，直接第三方工具，一两行代码搞定"></a>编数据的用到的里德-所罗门码：这里用了第三方库，虽然网上也能找到源代码，但有几十行，我就不添加了，直接第三方工具，一两行代码搞定</h5></li>
<li><h5 id="编格式用到的BCH码：代码有点少，所以用了源码"><a href="#编格式用到的BCH码：代码有点少，所以用了源码" class="headerlink" title="编格式用到的BCH码：代码有点少，所以用了源码"></a>编格式用到的BCH码：代码有点少，所以用了源码</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> reedsolo <span class="keyword">import</span> RSCodec</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_fmtEncode</span>(<span class="params">fmt</span>):</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;Encode the 15-bit format code using BCH code.&quot;&quot;&quot;</span></span><br><span class="line">    g = <span class="number">0x537</span></span><br><span class="line">    code = fmt &lt;&lt; <span class="number">10</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>, -<span class="number">1</span>, -<span class="number">1</span>):</span><br><span class="line">        <span class="keyword">if</span> code &amp; (<span class="number">1</span> &lt;&lt; (i + <span class="number">10</span>)):</span><br><span class="line">            code ^= g &lt;&lt; i</span><br><span class="line">    <span class="keyword">return</span> ((fmt &lt;&lt; <span class="number">10</span>) ^ code) ^ <span class="number">0b101010000010010</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_rsEncode</span>(<span class="params">data, per_block_data, number_of_block, per_block_err</span>):</span></span><br><span class="line">    <span class="comment"># Byte mode prefix 0100.</span></span><br><span class="line">    bitstring = <span class="string">&#x27;0100&#x27;</span></span><br><span class="line">    <span class="comment"># Character count in 8 binary bits.</span></span><br><span class="line">    bitstring += <span class="string">&#x27;&#123;:08b&#125;&#x27;</span>.<span class="built_in">format</span>(<span class="built_in">len</span>(data))</span><br><span class="line">    <span class="comment"># Encode every character in ISO-8859-1 in 8 binary bits.</span></span><br><span class="line">    <span class="keyword">for</span> c <span class="keyword">in</span> data:</span><br><span class="line">        bitstring += <span class="string">&#x27;&#123;:08b&#125;&#x27;</span>.<span class="built_in">format</span>(<span class="built_in">ord</span>(c.encode(<span class="string">&#x27;iso-8859-1&#x27;</span>)))</span><br><span class="line">    <span class="comment"># Terminator 0000.</span></span><br><span class="line">    bitstring += <span class="string">&#x27;0000&#x27;</span></span><br><span class="line">    res = <span class="built_in">list</span>()</span><br><span class="line">    <span class="comment"># Convert string to byte numbers.</span></span><br><span class="line">    <span class="keyword">while</span> bitstring:</span><br><span class="line">        res.append(<span class="built_in">int</span>(bitstring[:<span class="number">8</span>], <span class="number">2</span>))</span><br><span class="line">        bitstring = bitstring[<span class="number">8</span>:]</span><br><span class="line">    total_data_length = per_block_data * number_of_block</span><br><span class="line">    <span class="comment"># Add padding pattern.</span></span><br><span class="line">    <span class="keyword">while</span> <span class="built_in">len</span>(res) &lt; total_data_length:</span><br><span class="line">        res.append(<span class="built_in">int</span>(<span class="string">&#x27;11101100&#x27;</span>, <span class="number">2</span>))</span><br><span class="line">        res.append(<span class="built_in">int</span>(<span class="string">&#x27;00010001&#x27;</span>, <span class="number">2</span>))</span><br><span class="line">    <span class="comment"># Slice to 60 bytes for V6-H.</span></span><br><span class="line">    res = res[:total_data_length]</span><br><span class="line"></span><br><span class="line">    ecc = RSCodec(per_block_err)</span><br><span class="line">    blocks = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(number_of_block):</span><br><span class="line">        block = ecc.encode(res[:per_block_data])</span><br><span class="line">        blocks.append(block)</span><br><span class="line">        res = res[per_block_data:]</span><br><span class="line">    <span class="comment"># 先拼接数据，再拼接纠错编码</span></span><br><span class="line">    final_res = <span class="string">&#x27;&#x27;</span></span><br><span class="line">    <span class="comment"># 如果使用其他版本，可能不同数据块长度不同，请注意，这里四个数据块长度都是43</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(per_block_data):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>):</span><br><span class="line">            final_res = final_res + <span class="string">&#x27;&#123;:08b&#125;&#x27;</span>.<span class="built_in">format</span>(blocks[j][i])</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(per_block_data, per_block_data+per_block_err):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>):</span><br><span class="line">            final_res = final_res + <span class="string">&#x27;&#123;:08b&#125;&#x27;</span>.<span class="built_in">format</span>(blocks[j][i])</span><br><span class="line">    <span class="keyword">return</span> final_res</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="注意：BCH码最后写回字符串时的第一个字符是二进制的高位，所以要反转一下"><a href="#注意：BCH码最后写回字符串时的第一个字符是二进制的高位，所以要反转一下" class="headerlink" title="注意：BCH码最后写回字符串时的第一个字符是二进制的高位，所以要反转一下"></a>注意：BCH码最后写回字符串时的第一个字符是二进制的高位，所以要反转一下</h5><p><img src="https://img-blog.csdnimg.cn/20200307163303558.png" alt="在这里插入图片描述"><br><img src="https://img-blog.csdnimg.cn/20200307163308805.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzE3MTkwMTIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"><br>数据进行编码时，最终数据 = 4位编码格式 + 字符长度(这里刚好是8位) + 原数据的二进制 + 4位结束符</p>
<p>分块时，将最终数据分成若干份，再各自进行里德-所罗门码的编码，然后再按规定顺序重新组合排列</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>代码：<a href="https://github.com/jayying007/QR_Code">https://github.com/jayying007/QR_Code</a></li>
<li>前两天一窍不通，只觉得二维码挺有趣的，有点像密码学，到今天才大致理解其原理</li>
<li>后期两种纠错编码可以去了解一下</li>
<li>后期可以进一步优化，如添加logo，更改形状和颜色等，只要能识别就可以</li>
<li>其实这只是第一步，后面还有两步：拍照时定位到二维码并做预处理，将二维码还原为原始信息</li>
</ul>
<p>中间有遇到很难理解的部分，也想过放弃（那会要准备实习，得疯狂补知识），但好在坚持了下来，花费了两天多时间我觉得值。</p>
]]></content>
      <categories>
        <category>其他</category>
      </categories>
      <tags>
        <tag>二维码</tag>
      </tags>
  </entry>
  <entry>
    <title>分享一道算法设计题</title>
    <url>/2021/08/02/%E5%88%86%E4%BA%AB%E4%B8%80%E9%81%93%E7%AE%97%E6%B3%95%E8%AE%BE%E8%AE%A1%E9%A2%98/</url>
    <content><![CDATA[<h3 id="题目"><a href="#题目" class="headerlink" title="题目"></a>题目</h3><p>有一个函数rand()能生成1-65536之间的整数，利用这个函数，让30万人中有5万人中奖，要求保证公平（即每个人的中奖几率一样）。</p>
<p>|</p>
<p>|</p>
<p>|</p>
<p>|</p>
<p>|</p>
<p>|</p>
<p>|</p>
<p>|</p>
<p>|</p>
<h3 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h3><h4 id="首先解决如何做到30万人抽出5万人，还保证公平。"><a href="#首先解决如何做到30万人抽出5万人，还保证公平。" class="headerlink" title="首先解决如何做到30万人抽出5万人，还保证公平。"></a>首先解决如何做到30万人抽出5万人，还保证公平。</h4><p>假设能实现一个函数randX()，他能返回1-30万之间的整数，那么我可以制定一个策略，从随机到的数向后取5万个。如下图所示</p>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1556579/1615472709130-4d0d4a54-46a1-45e5-8322-ba832fddb6de.png" alt="img"></p>
<p>当后面不足5万个时，从头部补一些</p>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1556579/1615472845433-aba9cff7-fe04-49b4-90e2-cb071439853b.png" alt="img"></p>
<p>这种方式真的能保证公平吗，让我们来验证一下</p>
<p>每个人的中奖概率为 5万/30万 = 1/6</p>
<p>上面的这种方法，红色部分共有30万种可能（初始位置从1-30万），而每一个位置，比如5万这个位置，红色部分共有5万种可能会覆盖到这个数，所以这个位置被选到的概率也是 5万/30万 = 1/6</p>
<h4 id="现在问题转为如何设计一个randX-，使其返回1-30万之间的整数，且公平"><a href="#现在问题转为如何设计一个randX-，使其返回1-30万之间的整数，且公平" class="headerlink" title="现在问题转为如何设计一个randX()，使其返回1-30万之间的整数，且公平"></a>现在问题转为如何设计一个randX()，使其返回1-30万之间的整数，且公平</h4><p>一个错误方式是将rand() * (300000/65536)，这种方式虽然能得到1-300000之间的整数，但是有些数却永远取不到。</p>
<p>rand()=1时，rand()*(300000/65536)=4.5776,那么无论如何你就取不到1，2，3了</p>
<p>那么该如何解决呢？一种方法是分区间</p>
<p>比如把30万划分为5个一样大的区别，每个区间只有6万</p>
<p><img src="https://cdn.nlark.com/yuque/0/2021/jpeg/1556579/1615474782637-741cfe45-dcba-4c01-86b4-8ae1910a1c29.jpeg" alt="img"></p>
<p>我们先随机取1-5之间的数，从而确定在那个区间取。</p>
<p>一个区间只有6万，所以我们可以用rand()来取1-6万之间的整数了，最后加上区间起始位置的数就可以了。</p>
<p>这样就没问题了吗？</p>
<p>其实如果选择不慎，还是不公平的。</p>
<p>比如一个能生成1-10随机数的函数rand，用它来生成1-4的随机数，如果写成rand%4+1，那结果就是错的。</p>
<p>我们可以列出所有计算的可能情况</p>
<p>生成1: 1 5 9</p>
<p>生成2: 2 6 10</p>
<p>生成3: 3 7</p>
<p>生成4: 4 8</p>
<p>可见生成不同随机数的概率其实是不一样的，生成1的概率是十分之三，生成4的概率是十分之二。</p>
<p>要实现一个公平的随机数生成器，需要保证生成的最大数为原来随机器最大数的因数。比如1-10对应能制造生成1-2和1-5两种随机器，且保证公平。</p>
<p>而针对上面这种情况，我们能做的是拒绝采样（reject sampling），当得到的数为9或10时，让它重新生成一个随机数直到满足条件。</p>
<p>对于本文的题，65536的因数都是2的指数次幂，而30万不是，所以也只能采用拒绝采样的方法。</p>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
  </entry>
  <entry>
    <title>基于消息表的分布式事务</title>
    <url>/2021/08/02/%E5%9F%BA%E4%BA%8E%E6%B6%88%E6%81%AF%E8%A1%A8%E7%9A%84%E5%88%86%E5%B8%83%E5%BC%8F%E4%BA%8B%E5%8A%A1/</url>
    <content><![CDATA[<p><strong>在分布式数据库中，通过避免强一致性可以带来伸缩性的巨大提升。</strong></p>
<h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>随着你公司Web应用的发展，数据库容易陷入瓶颈。通常有两种方法解决这个问题，垂直扩展和水平扩展。</p>
<ul>
<li>垂直扩展通过提升硬件性能，但一般比较昂贵。</li>
<li>水平扩展是本文主要讨论的方法，相比垂直扩展要复杂一些。</li>
</ul>
<p>水平扩展的方式通常也有两种：按模块切分，数据表做切片（Sharding）</p>
<ul>
<li>按模块切分的方式，将 Users, products, and transactions切分到不同的数据库。不同模块通过设计合适的外键进行关联访问。</li>
<li>每个模块可以根据具体容量再进行切分</li>
</ul>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1556579/1612706617125-07dd0bb7-f24e-4c31-9449-e7c3e9ee74c1.png" alt="img"></p>
<h3 id="ACID与BASE"><a href="#ACID与BASE" class="headerlink" title="ACID与BASE"></a>ACID与BASE</h3><p>我们知道，在单个数据库下，是可以实现ACID的。而在跨多个数据库的情况下，一般是通过2PC实现。2PC保证了强一致性，而根据CAP理论可以得知，其可用性则无法完全实现。</p>
<p>那如果我们想要可用性，不要求强一致性呢？可以试试BASE。</p>
<p>如果说ACID是悲观的，要求每个操作都严格一致，那么BASE则是乐观的，允许流动（flux）的状态。</p>
<p>BASE的高可用并不是保证每个用户请求都被执行。比如有5台数据库服务器，当有一台服务器宕机了，那么大约只有20%的用户访问不了，这种情况下我们仍然认为系统是可用的。</p>
<p>下面我们以一个例子演示从单机到多机的变化。</p>
<h3 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h3><p>假设我们有以下数据表User和Transaction，它们存放在同一个数据库中。每当有一件商品卖出时，向Transaction表插入一条数据，然后更新买家和卖家的user表。</p>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1556579/1612708581417-c4fcd3cb-fe28-4adc-82e8-30552553f1dd.png" alt="img"></p>
<p>上面这个过程可以通过下面的SQL事务表示</p>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1556579/1612708777051-5ff679c3-47a6-4bba-80c8-672b994dee77.png" alt="img"></p>
<p>当我们按模块划分后，user和transaction位于不同的数据库服务器上，此时再执行则变成了两个独立的事务。</p>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1556579/1612709344023-95ba9210-1d7f-4d85-8b2f-d17041173c7f.png" alt="img"></p>
<p>这种情况下，若事务1执行成功，事务2执行失败，就出现了数据不一致的情况。怎么解决这种问题呢？这里就引入了我们本文的主角，消息表。</p>
<p>实现消息表的方法可能有很多，但不管怎样，我们需要将消息的执行也划归到我们的事务内。引入消息表后，我们的SQL语句会变成下面这样。</p>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1556579/1612709759494-0704876f-bc14-4a72-8f01-5c3985384ff6.png" alt="img"></p>
<p>可以创建一个后台进程，查询消息表，然后根据消息内容更新user表。</p>
<p>不过上述的代码其实有问题，因为消息表是和transaction表一起的，跟user表在不同的数据库服务器上，如果按照上面那种方法，一个事务中有update消息表和user表，又回到了使用2PC。</p>
<p>如何解决这个问题？这里要先引入一个概念，幂等性。</p>
<p>如果一个操作执行一次或几次的结果都是一样的，那么它就是幂等的。如果一个操作是幂等的，那么可以实现：即使操作失败了，我们可以重试直到成功，而重试不会影响最终结果。</p>
<p>我们上面的这个例子则不支持幂等，每次操作都会改变购买量或销售量。怎么解决呢？可以在user表所在数据库服务器上再加一个表</p>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1556579/1612710768983-7970e275-52d4-4a75-b846-030d42ecc7bd.png" alt="img"></p>
<p>然后将SQL语句改为下面这种</p>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1556579/1612710796277-0112dc19-e601-4a1b-b7f0-838c2775bb68.png" alt="img"></p>
<p>从上面的SQL语句中可以看出，message被移到了事务外面，这样使得只有在事务操作成功的时候才将其从消息表中移除。</p>
<p>当事务执行成功，在移除消息表之前宕机，重启之后又重新消费这条消息。而事务中有对已消费信息的判断，从而解决了重复消费的问题。这样保证了数据在最后能达到一致性。</p>
<h3 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h3><p><a href="https://queue.acm.org/detail.cfm?id=1394128">https://queue.acm.org/detail.cfm?id=1394128</a></p>
]]></content>
      <categories>
        <category>系统架构</category>
      </categories>
      <tags>
        <tag>分布式</tag>
        <tag>消息表</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机简史（一）</title>
    <url>/2021/08/15/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%AE%80%E5%8F%B2%EF%BC%88%E4%B8%80%EF%BC%89/</url>
    <content><![CDATA[<h2 id="这个系列什么？"><a href="#这个系列什么？" class="headerlink" title="这个系列什么？"></a>这个系列什么？</h2><p>我觉得，既然是学计算机的，那么应该对计算机体系有一些认识。当然，这跟你会不会修电脑无关。。。</p>
<p>作为计算机专业的，计算机的一些核心课程想必都学过（虽然我们学院没开编译原理、数字电路这些课程。。），但是很少把它们串起来的。这里是我当时准备面试前，把自己对计算机的认知体系梳理了一遍，也是对我大学四年学的东西的总结，希望对你也有帮助。当然，由于个人能力有限，可能有些地方讲的不大对，所以也请你辩证地看。</p>
<p>这里我不会特别深入讲某个东西，但是我会推荐一些不错的资源，那些要学习的话也是挺费时间的。</p>
<p>另外，这个系列更多的是总结，而不是零基础入门。</p>
<span id="more"></span>

<h2 id="从逻辑门到一台计算机"><a href="#从逻辑门到一台计算机" class="headerlink" title="从逻辑门到一台计算机"></a>从逻辑门到一台计算机</h2><h3 id="电的产生及应用"><a href="#电的产生及应用" class="headerlink" title="电的产生及应用"></a>电的产生及应用</h3><blockquote>
<p> 这部分推荐一个B站Up主：《Ele实验室》，他的视频都是计算机相关的，通过动画呈现，小白也听得懂。</p>
<p>视频在这里：<a href="https://www.bilibili.com/video/BV1774114798/?spm_id_from=333.788.recommend_more_video.0">https://www.bilibili.com/video/BV1774114798/?spm_id_from=333.788.recommend_more_video.0</a></p>
</blockquote>
<p>物质发生化学反应，产生的溶液中存在阴阳离子。不同金属吸引不同阴阳离子，形成电池的正负极，电池内部不知道有什么结构，使得正负离子能够位于电池两极，从而形成了电压。而当我们连接上导线的时候，电子就能顺着这条导线会到正极，电子的流动我们称为电流。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/1608167111154-0f2f41cb-42a8-4775-b73a-41d7564f406e.png" alt="image.png"></p>
<p>当然，也可以利用切割磁感线的原理产生电流。说到磁，学了物理我们知道，有电流通过的线圈会产生磁场。那么基于这个原理我们可以设计这样一个电路。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/1608167333744-b1badb83-b325-4f6d-9288-e56e693c9518.png" alt="image.png"></p>
<p>当红色电路接通时，线圈产生的磁场把黑色电路的开关吸下来，从而电路闭合，灯泡亮起。这种结构一般称为继电器，可以实现电路影响另一条电路。假设黑色电路是高电压，那我们通过红色电路的低电压（emm,一般不会是干电池）就能控制高电压的电路。</p>
<h3 id="逻辑门"><a href="#逻辑门" class="headerlink" title="逻辑门"></a>逻辑门</h3><p>利用上面的这个磁原理，我们其他能实现很多事情了。比如构造出非门、与门、或门等。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/1608168149593-2d1fdfc6-b233-4fdb-b688-f06686b1bef7.png" alt="image.png"></p>
<p>有了这些基础门，其他更高级的元器件也能实现了。比如加法器、选择器、反向选择器、ALU，直到CPU。</p>
<p>如果你想学习如何组装这些组件，这里有一个网站：<a href="https://nandgame.com/%EF%BC%8C%E8%BF%99%E4%B8%AA%E7%BD%91%E7%AB%99%E5%83%8F%E7%8E%A9%E6%B8%B8%E6%88%8F%E4%B8%80%E6%A0%B7%EF%BC%8C%E8%AE%A9%E4%BD%A0%E4%BB%8E%E9%80%BB%E8%BE%91%E9%97%A8%E4%B8%80%E6%AD%A5%E6%AD%A5%E7%BB%84%E8%A3%85%E5%88%B0%E4%B8%80%E5%8F%B0%E8%AE%A1%E7%AE%97%E6%9C%BA%E3%80%82">https://nandgame.com/，这个网站像玩游戏一样，让你从逻辑门一步步组装到一台计算机。</a></p>
<p>如果你更倾向于看书，这里推荐一本不错的书：《计算机系统要素》，里面的电路组件设计是通过HDL（硬件描述语言）来实现的，我之前也做过一些笔记，可以看看：<a href="https://github.com/jayying007/nand2tetris">https://github.com/jayying007/nand2tetris</a></p>
<h3 id="组计算机"><a href="#组计算机" class="headerlink" title="组计算机"></a>组计算机</h3><p>接下来，你就要把这些元器件组装起来，构成一台计算机了，比如下面的图示：</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/1608168660703-e43ec6d5-7565-4318-bf1e-61e6f71c0363.png" alt="image.png"></p>
<p>更多的组装细节，还是看书《计算机系统要素》吧。基本上，计算机干的事就是</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">while(true) &#123;</span><br><span class="line">	取指令</span><br><span class="line">	译码（根据指令生成控制信号）</span><br><span class="line">	执行（运算、读写内存...）</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>到这里，你已经拥有了一台自动机。当你接通电源的时候，它会从内存的0地址处开始读指令，执行…</p>
<p>这里还有一本不错的书推荐，也是从逻辑门讲到计算机的，读起来挺有趣的，上面的图也是从这里取的。</p>
<p><img src="https://blog-img-1256061645.cos.ap-guangzhou.myqcloud.com/image-20210815183553332.png" alt="image-20210815183553332"></p>
<p>另外，如果有玩《我的世界》的小伙伴，也可以尝试用红石电路去构建。比如我之前就自己搭了一个二进制加法器，地址在这里：<a href="https://www.bilibili.com/video/BV1R3411z7ew%E3%80%82">https://www.bilibili.com/video/BV1R3411z7ew。</a></p>
<blockquote>
<p>但是最后还有个问题，内存一开始也没有数据呀，那这些指令怎么来的呢？这部分内容等下篇博客来解答。</p>
</blockquote>
]]></content>
      <categories>
        <category>计算机简史</category>
      </categories>
  </entry>
</search>
